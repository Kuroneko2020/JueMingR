# JueMingR Agent 入口

本文件是 Codex 和其他 Agent 的项目总入口及路由表，应纳入 Git。这里只记录稳定的项目级信息和路由，不记录动态任务进度。

## 当前阶段

当前处于 **Phase 0：项目考古、需求整理、规则与架构设计**。

未经项目所有者批准，Phase 0 禁止创建生产代码、项目骨架或第三方依赖。

## 跨会话冷启动

新对话、上下文不完整或任务交接时，在写入前必须依次读取：

1. [`docs/README.md`](docs/README.md)；
2. [`docs/计划/稳定起步路线图.md`](docs/计划/稳定起步路线图.md)；
3. [“JueMingR：稳定起步总跟踪”Issue](https://github.com/Kuroneko2020/JueMingR/issues/7)；
4. 总跟踪 Issue 指向的当前任务 Issue；
5. 当前任务路由要求的规范与 ADR。

聊天记忆、聊天摘要和本地 handoff 不是权威事实源。当前阶段以总跟踪 Issue 为准，当前任务状态与授权以具体任务 Issue 为准。仓库、路线图、总跟踪 Issue 或当前任务 Issue 互相冲突时，停止写入并并列报告冲突，不得自行裁决。普通任务不得自行改变稳定起步路线图。

## 项目边界

可写项目：

- 本地：当前 Git 仓库根目录（必须通过 `git rev-parse --show-toplevel` 确认，不得依赖硬编码的个人目录）
- GitHub：`Kuroneko2020/JueMingR`

永久只读 Legacy：

- 本地：当前仓库根目录的同级目录 `../JueMingZ`
- GitHub：`Kuroneko2020/JueMingZ`

若 `../JueMingZ` 不存在，Agent 必须停止并报告；不得自行猜测路径、创建目录、克隆副本或改用其他本地项目。

禁止修改、提交、格式化或自动修复 Legacy。禁止让 JueMingR 在编译期或运行时引用 JueMingZ。

旧项目只能作为功能、行为、兼容性、测试案例和历史问题的证据来源。旧代码中存在某种行为，不等于新版必须保留该行为。

## 事实来源优先级

1. 项目所有者明确决定；
2. JueMingR 正式产品契约和架构决策；
3. 用户确认的 Legacy 行为；
4. Legacy 自动测试；
5. Legacy 实际代码；
6. Legacy 历史文档。

Agent 必须明确区分：已确认事实、推断、建议、待决定事项。

## 工作与文档路由

- 修改前先阅读 [`docs/README.md`](docs/README.md)，并根据其中路由读取完成任务所必需的文档。
- 文档类型、权威、生命周期、Git 边界和结构演进规则见 [`docs/规范/文档治理规则.md`](docs/规范/文档治理规则.md)；重要结构决策见 [`docs/决策/`](docs/决策/)。
- 涉及写入任务、风险分级、任务契约、Issue、分支、commit、PR、独立审查、合并授权、停止条件或长期工具/角色依赖的工作，必须阅读 [`docs/规范/工程治理规则.md`](docs/规范/工程治理规则.md)。
- 涉及 Legacy 功能考古、行为恢复、平替、兼容差异或用户验收的任务，必须阅读 [`docs/规范/Legacy平替与行为契约规则.md`](docs/规范/Legacy平替与行为契约规则.md)。
- 涉及运行平台、Terraria 版本、Windows/渠道兼容、加载链、依赖分发、安装恢复或多人拓扑的任务，必须阅读 `docs/规范/技术平台与兼容规则.md`。
- 涉及总体架构、模块职责、项目依赖方向、状态所有权、Runtime/Session 生命周期、Composition Root、游戏 Observation/Operation、Feature 接入、UI、持久化、Diagnostics 或项目骨架映射的任务，必须阅读 [`docs/设计/总体架构与状态所有权.md`](docs/设计/总体架构与状态所有权.md)。
- 涉及性能风险、性能声明、基线或测量、功能关闭成本、缓存/快照/队列、日志与 I/O，或一次性测量工具的任务，必须阅读 [`docs/规范/性能治理规则.md`](docs/规范/性能治理规则.md)。
- 涉及编译、测试、审计、证据结论、测试期待修改、mock/stub/fake、Terraria 游戏内实机、多人验证、测试数据隔离、flaky、覆盖率或慢检查的任务，必须阅读 [`docs/规范/测试与审计规则.md`](docs/规范/测试与审计规则.md)。
- 涉及产品版本、构建身份、开发包或测试包、配置 schema 与迁移、Terraria 兼容发布记录、Tag、GitHub Release、发布资产、SHA-256、发布说明、撤回或替代发布的任务，必须阅读 [`docs/规范/版本配置与发布规则.md`](docs/规范/版本配置与发布规则.md)。
- 普通任务不得自行新增、删除、重命名或重组 `docs` 一级目录；需要调整时必须停止并取得项目所有者批准。
- 需要查询动态任务状态时，以 GitHub Issues 为准，不得从稳定文档推断。
- 每个任务保持单一目标，不得夹带无关重构。
- 遇到规则冲突、需求不明、需要扩大范围或修改架构时，停止并报告，不得自行猜测。
- 禁止把测试通过、审计通过、文档更新或版本推进单独称为产品进展。
- 具体规范、设计和决策由 `docs/` 下的正式文档按需承载，不在本文件复制全文。
