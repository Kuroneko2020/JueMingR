# JueMingR Agent 入口

本文件是 Codex 和其他 Agent 的项目总入口及路由表，应纳入 Git。这里只记录稳定的项目级信息和路由，不记录动态任务进度。

## 当前阶段

当前处于 **Phase 0：项目考古、需求整理、规则与架构设计**。

未经项目所有者批准，Phase 0 禁止创建生产代码、项目骨架或第三方依赖。

## 项目边界

可写项目：

- 本地：`C:\Users\kongd\Desktop\JueMingR`
- GitHub：`Kuroneko2020/JueMingR`

永久只读 Legacy：

- 本地：`C:\Users\kongd\Desktop\JueMingZ`
- GitHub：`Kuroneko2020/JueMingZ`

禁止修改、提交、格式化或自动修复 Legacy。禁止让 JueMingR 在编译期或运行时引用 JueMingZ。

旧项目只能作为功能、行为、兼容性、测试案例和历史问题的证据来源。旧代码中存在某种行为，不等于新版必须保留该行为。

## 事实来源优先级

1. 项目所有者明确决定；
2. JueMingR 正式产品契约和架构决策；
3. 用户确认的 Legacy 行为；
4. Legacy 自动测试；
5. Legacy 实际代码；
6. Legacy 历史文档。

Agent 必须明确区分：已确认事实、推断、建议、待决定事项。

## 工作与文档路由

- 修改前先阅读 [`docs/README.md`](docs/README.md)，并根据其中路由读取完成任务所必需的文档。
- 文档类型、权威、生命周期、Git 边界和结构演进规则见 [`docs/规范/文档治理规则.md`](docs/规范/文档治理规则.md)；重要结构决策见 [`docs/决策/`](docs/决策/)。
- 普通任务不得自行新增、删除、重命名或重组 `docs` 一级目录；需要调整时必须停止并取得项目所有者批准。
- 需要查询动态任务状态时，以 GitHub Issues 为准，不得从稳定文档推断。
- 每个任务保持单一目标，不得夹带无关重构。
- 遇到规则冲突、需求不明、需要扩大范围或修改架构时，停止并报告，不得自行猜测。
- 禁止把测试通过、审计通过、文档更新或版本推进单独称为产品进展。
- 具体规范、设计和决策由 `docs/` 下的正式文档按需承载，不在本文件复制全文。
