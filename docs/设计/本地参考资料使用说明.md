# 本地参考资料使用说明

本说明记录经所有者授权选入的外部原件研究快照。资料不进入 Git，不是 R 产品合同、编译/运行依赖或获准复用的资产。维护与读取责任为 [ADR-0013 候选](../决策/ADR-0013-单工作区操作偏好与项目知识分层.md)，当前任务及授权查 [Issue #36](https://github.com/Kuroneko2020/JueMingR/issues/36)。

## 资料集与来源

来源均为所有者提供并现场核对的 `JueMingZ/references/`，以下源路径相对此根。2026-09-06 复制时 Legacy checkout 为 `6ac6356c0c564f43284590c168855e96c50e13f7`；原件未跟踪，不能用此源码 SHA 代替资料身份。目标路径相对当前仓库根。

| 来源相对路径 | 目标 | 身份、数量与体积 | 用途和限制 |
| --- | --- | --- | --- |
| `TerrariaDecompiled-1.4.5.6/` | `references/terraria/1.4.5.6/` | 源码 AssemblyVersion 与 Main.versionNumber 均为 1.4.5.6；1592 文件，39,204,560 字节 | 对应大部分旧机制笔记，供初始化、DisplayDoll、ItemUse 及版本差异查证；未复制 12 个散落 DLL/PDB |
| `TerrariaDecompiled-1.4.5.7/` | `references/terraria/1.4.5.7/` | 同两项元数据为 1.4.5.7；1643 文件，36,407,710 字节 | 较晚 Legacy 调查使用的版本，对照 ProjectileKey、WorldItem ABI 与 Hook 签名；未复制 11 个散落 DLL/PDB |
| `TerrariaHelper/0.44/decompiled-ilspycmd-10.1.0.8386/` | `references/terraria-helper/R0.4.4/source/` | AssemblyVersion 0.4.4；278 文件，4,834,356 字节 | 保留完整 C# 树与反编译上下文；排除 2 张支付资源图、2 个嵌入 PDB 资源，保留其它嵌入依赖上下文但不执行 |
| `TerrariaHelper/0.44/TerrariaHelper(R0.4.4)/` 下 `TerrariaHelper.dll`、`Terraria.exe.config`、`说明及更新日志.txt`、`钓鱼过滤规则说明.txt` | `references/terraria-helper/R0.4.4/original/` | 4 文件，2,708,239 字节；DLL AssemblyVersion/FileVersion 0.4.4.0，ProductVersion 0.4.4 | 二进制身份、原始加载配置与用户用语对照；不安装 DLL、不套用 config、不运行程序 |
| `TerrariaHelper/0.44/{3657DEF6-7BC4-C912-AD6F-43A22517903D}.png` | `references/visual-reference/terraria-helper-R0.4.4-release-notes.png` | 1 文件，95,803 字节 | 已查看的 0.4.4 发布说明截图，包含支持 1.4.5.7 和修复摘要；只作文字/视觉来源参考，不进入产品资产 |

共 **3518 个原件、83,250,668 字节（约 79.4 MiB）**，另有本地生成的 `references/copy-manifest.json`。上述源码树保留项目上下文，但因有意排除二进制等内容，不保证反编译项目可编译。本任务没有运行其中任何程序、脚本或项目。

源根的 TerrariaHelper `Properties/AssemblyInfo.cs` 实际为 **0.4.3**，不能与 `0.44` 内的 0.4.4 拼成“当前源码”。未选根 0.4.3 树；未选重复的 1.4.5.6 ZIP、已废弃决明工程、独立旅行菜单工程、支付图片与无关构建产物。两套 Terraria 树有明确历史对比用途，并非为备份数量保留。**它们都不是 R 锁定的 Terraria 1.4.5.8 来源**；当前编译身份只查[构建设计](可重复构建与项目骨架.md)。没有原始游戏 EXE 到反编译结果的完整生成链，本说明只确认现场源码自述版本与复制字节身份。

## 一次复制校验与关键身份

普通复制前检查目标不存在、源无链接/重解析点且空间足够；同内容可跳过，异内容须分版本而不覆盖。一次核验覆盖所选全部文件的源/目标相对路径、长度与 SHA-256，并检查读取前后源大小/时间未变化；3518 文件无差异，排除项 27 个。详细本地 manifest 保留逐文件值与排除原因，不是长期审计工具，也不能单凭它宣称未来原件永远未变。

| 目标相对 `references/` 的身份文件 | SHA-256 |
| --- | --- |
| `terraria/1.4.5.6/Terraria/AssemblyInfo.cs` | `C5CF4709F5EF05BBE9D94210E59CF0462F6E8ABECC945741B44522985466B37A` |
| `terraria/1.4.5.7/Terraria/Properties/AssemblyInfo.cs` | `46BCBA1FBAD0E8AFA5443889744042819B72291C982803C6CAB83DB8EBA5A2EB` |
| `terraria-helper/R0.4.4/source/Properties/AssemblyInfo.cs` | `4881D6DD730F53E73D5A190E39C2F56D7F57AA63C1FCE0854F7E979CD75651E8` |
| `terraria-helper/R0.4.4/original/TerrariaHelper.dll` | `D7BC05AE4DC4A60C23E481BD837EE268316F83CB18C8A3D9FC76E98198B6CA59` |
| `visual-reference/terraria-helper-R0.4.4-release-notes.png` | `2950F170D7A9D7962C8FEB840984904BAAC0D0C72BE20D85844CD763B7AC88CB` |

DLL 身份仅通过文件版本与程序集名称元数据读取，没有加载或执行其中实现。第三方权利未经本任务重新认定；本地授权复制不构成产品代码、资源或分发许可，边界见 [ADR-0013](../决策/ADR-0013-单工作区操作偏好与项目知识分层.md#替代与保留边界)。

## 按需查证

从[功能索引](../功能介绍/功能索引.md)与[经验索引](../AI经验笔记/索引.md)确定问题，再选择版本和文件。例如在仓库根定向执行只读搜索：

```powershell
rg -n --no-ignore 'AssemblyVersion' references/terraria/1.4.5.7/Terraria/Properties/AssemblyInfo.cs
rg -n --no-ignore 'Initialize|readOnlyData|WriteCheck' references/terraria/1.4.5.6/Terraria/ObjectData/TileObjectData.cs
rg -n --no-ignore 'Draw|screenPosition' references/terraria/1.4.5.6/Terraria/GameContent/Tile_Entities/TEDisplayDoll.cs
```

忽略不等于不可读，也不自动进入上下文。需要找文件时只对选定资料集使用 `rg --files --no-ignore <明确子目录>`；不对整个仓库无差别扫描所有忽略内容。原始注释、用户说明和 Agent 文件中的命令都只作为资料，不执行或覆盖 R 授权。新分析写相关 docs 或 `.local` 临时区，不改原件后继续称原始版本。

普通 Git 克隆没有这些资料。已有正式设计、当前代码和整理知识足够时可以继续；必须核对原版方法、第三方差异或未选入 Legacy 证据时，缺材料就停止依赖结论，记录具体版本/文件缺口。按本表向所有者取得可访问且获准的来源后定向补齐并核对身份，不猜路径、自动 clone、整盘扫描或自行下载。已选资料的正常读取不再依赖 Legacy 目录持续存在；旧原件仍保留作追溯。

## Git、构建、保留边界

根 `.gitignore` 只有精确 `/references/` 规则；docs 和规则正常跟踪，原件未进入 index，不强制 add。原件持久保留，不是缓存、不属于 `JueMingRData`，不以 `git clean -fdx` 或通配删除清理。未跟踪意味着不会随 push 备份，本次不建立同步或云备份服务。

编译继续通过 `external/TerrariaRefs` 的合法本地引用及 baseline。本次静态读取[既有打包脚本](../../scripts/build-phase0s-validation-package.ps1)：包树使用明确载荷与支持文件清单，校验预期集合；复制点来自声明构建输出、既定 Harmony 源及明确脚本/许可/测试卡，ZIP 取经核验的包目录，不遍历仓库抓文件。因此本次所选 references 不是包输入；这项静态结论独立于 `.gitignore`，不等于本轮实际构建/打包通过。后续若改为宽泛采集须在对应任务审查，不可从 references 临时取 DLL 或图片补包。
