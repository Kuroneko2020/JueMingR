# 自动恢复、增益与邻近服务实现设计

对应 [#97](https://github.com/Kuroneko2020/JueMingR/issues/97) 与[行为合同](../功能介绍/自动恢复增益与邻近服务行为合同.md)。实现候选，实际状态查 Issue；本设计不自行宣布接受。

## 基线与证据

同工作区从 `main=5ea5ab644b3769ed3903007a5c6ba12bf7024c89` 建普通任务分支。#95 已关闭、#96 已合并并交还干净工作区；前序实际安装仍为 `98e31c56…`，不能拿文档合并 SHA 冒充已安装产物。调查完整读取《背包与消耗品》§4及§5+、《NPC服务与连续加工》§1—3，与总报告职责/剩余矩阵；历史文件不倒改。

当前原版 `Terraria.exe` 1.4.5.8、MVID `2c29f6c3-4bd9-4add-9c58-da159804e083`、SHA-256 `960A03BFF6050CF7BE16DFC1A7B19E10FC2C4F8F835A6A3B135A50DD9E6BA2F3`。本地只读摘录身份见 `.local/全功能调查/原版/提取身份.md`，不随产品分发。Legacy remote/冻结 HEAD 核验为 `Kuroneko2020/JueMingZ`、`6ac6356…`，永久只读。

| 原版窄入口 | 作用与不可误用点 |
| --- | --- |
| Player.QuickHeal / QuickMana | 完整原版效果、消费、特殊冷却；仅本次作用域替换 GetItemToUse 返回的真实提供者，作用域外保持手动与魔力花。 |
| GetItemManaUsageDetails + CheckManaPredictWithoutUse | 无扣款需求观察；CheckMana 即使 pay=false 仍会改 slowMagicUse，不能当纯查询。实验 ManaV2 按其原版预测分支。 |
| QuickBuff / PickBestFoodItem / ShouldUseItem | 单目标许可同时管食品和其它增益；ShouldUseItem 内含实际 TryStartUse/扣蓝，必须先过滤非目标，不试运行两遍。 |
| ApplyLifeAndOrMana / AddBuff / ItemCheck | 实际效果观察，保存消费前类型；不能拿一整帧差值或启动动画单独证明效果。 |
| Main.TryRemovingBuff(i,b) | 图标取消因果，后置确认真实移除；普通 DelBuff 不是人工意图。 |
| TileObjectData / IsInTileInteractionRange / TileInteractionsUse | 完整家具与当前原版距离；Check 已调用 Use，禁止 Check 后再 Use。自动 scope 仅抑制 InteractWithCraftingStation，普通手动放行。 |
| Main.GetNurseHealCost / NPCChatText_DoNurseHeal | 原版价格与真实付款治疗；无钱预检纯计数。当前零费不治疗。 |
| Main.NPCChatText_DoTaxCollector | 原版世界金币与客户端 RequestNewItem/报文21，余额归零不等于金币已入包。 |

## 所有者与职责

`Features/Recovery` 拥有纯选药、互斥/需求、有限失败准入及不可变设置；`TerrariaHost/Recovery` 分恢复品、手动跟随、家具、NPC服务和呈现适配，不把业务扩回 Runtime。现有 `Phase0SLoadChainHost` 只装配，在同一个 Runtime/Session 和合法游戏线程更新；公共输入与公共快捷键继续唯一所有。

恢复使用不选槽、不搬药、不清输入；保留真实 Item 数组/引用/槽与会话世代。`ItemOperationOwnership` 窄扩账户与槽域，消费和护士按真实写集认领；最终原版入口仍核验，不用一个 AutomaticOperation 布尔绕开所有保护。异常不回填数量或 Item 快照。自动使用与手动学习作用域明确区分。

药品、增益设置和 NPC/家具开关按自然域保存，沿用 `DocumentWorker` / `AtomicFileDocument`。新许可只在可靠提交后生效；关闭/移除在待保存期间先阻止旧资格。每域一个在途请求，有界排队/重试，不保存原版对象到 worker，不新建数据库或执行框架。

## 生命周期与性能卡

| 域 | 触发及规模 | 稳定/关闭成本和退休 |
| --- | --- | --- |
| 恢复 | 合法本地玩家每更新先检查缺口/费用/原版冷却，再有限主包58+虚空40候选 | OFF 无扫描；同次真实喝药防重复；未知来源保护至明确身份变化，不靠开关洗白。 |
| 目录 | 主动打开/重试时按 ItemID.Count 完整定义一次，actual type 去重 | Draw 不 SetDefaults；失败不缓存为空，不后台每帧重试。 |
| 增益 | 非空名单且有效开关时有界轮转；先现有 Buff 后来源 | 单项失败记录有界，以真实物品/效果/配置变化恢复；空名单/OFF不扫库存。 |
| 跟随 | 真实手动使用/取消入口且对应跟随开启 | 早退不扫描；局部前后态只覆盖一次动作，不追踪自然消失。 |
| 家具 | 缺七种效果时有限交互区域分步观察，缓存最近候选 | 已有全部效果不查 Tile；同位置完整循环后间隔复查，移动/修改候选重验。会话退役清候选。 |
| NPC | 有治疗需求/正税款才有限发现；已知目标直接复核 | 无钱观察支付能力而不点击；失败/未知不循环；真实身份变化退休，临时 unavailable 不等于新会话。 |

动作前重读与持续施法及时性不能用极长轮询换取计数低。工作量计数仅 DEBUG 检查；无生产常驻探针、签名字串或普通状态日志。共享冲突按合同优先，独立分支继续。

## 小步计划与验证

1. 提交本合同/设计/路由，然后策略表先 RED→GREEN；实现恢复、许可目录、真实源和持续施法，接入最窄原版隔离用例及 UI，提交。
2. 白名单与跟随实际事件；验证最后一件、手动取消/自然消失、多个缺失与失败轮转，提交。
3. 七种完整家具、有限观察及制作窗口窄抑制；真实原版作用域/手动窗口回归，提交。
4. 护士纯报价/账户占用/失败准入和对话所有权；隔离钱包与银行支付、未知/零费和无钱稳定，提交。
5. 原版税收、防重放、公共冲突和生命周期；完整配置/UI/工作量路由，相邻输入/物品/存钱回归，提交。
6. 最终整体独立只读审查一次；修订仅核增量与真实影响。固定 clean head，现有正常构建和完整包门，同工作区顺序两次干净构建/ZIP比对。检查进程、原安装/备份和用户数据保护后仅一次安全安装，保留候选等待所有者。

纯策略验证全部合同表，不用 fake 效果冒充原版消费。受控原版测试在独立临时目录与合成玩家/世界数组中执行，禁止读写真实存档或消费真实物品；保留原施法/鼠标状态测试。界面检查真实字体与主窗布局，普通状态不变高。实际网络只在已授权拓扑验证，源码同步路径与隔离 netMode1 不等于联机通过。

正常 build/workload 入口新增 recovery 局部组；共享输入/F5/文件/物品占用改动扩选真实消费者，保留 About 局部组。验证实际执行组而非只登记名字。安装、包、源、验证身份分别记在 Issue/PR，技术文档只保留可维护事实，不写会话流水。
