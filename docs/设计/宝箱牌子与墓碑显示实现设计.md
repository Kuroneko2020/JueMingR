# 宝箱牌子与墓碑显示实现设计

状态：所有者已授权实施；实现、验证、安装和体验接受分别记录在 [Issue #63](https://github.com/Kuroneko2020/JueMingR/issues/63)。前置需求 [PR #62](https://github.com/Kuroneko2020/JueMingR/pull/62) 已合并。本文是本次有限实现设计，不改变路线图或长期治理。

行为依据：[功能行为合同第 7 节](../功能介绍/宝箱牌子与墓碑显示功能行为合同.md#7-已确认的新版首轮合同)。正式合同优先于本设计的内部选择；历史 V0 仅作证据。

## 责任与数据流

- `Features/WorldObjectText` 拥有三个显示模式、对象几何规则、候选选择、开过位置语义及空间索引；不引用 Terraria 或 Notes。
- `Platform/WorldObjectText` 只承载同步只读值和窄观察端口。游戏原始 tile/chest/sign 事实始终属于原版。
- `TerrariaHost/WorldObjectText` 读取当前 `.8` 原版字段，观察本地成功开箱，准备原版文字和图标，按既有世界层画已准备结果。共享世界读取只共享字段和当前 tick 缓存；金属探测门只适用于需要它的消费者。
- `Features/Text` 提取现有 Notes 的有限 Unicode 边界规则，保持 Notes 行为；世界标签以有界游标逐段消费，不能先建立整个长字符串的边界数组。
- 开过记录 owner 把无游戏对象的位置 DTO 交给一个进程级领域 I/O 调度器。调度器以线程池上的单个在途操作顺序复用 `AtomicFileDocument`，负责所有 pair 的读、合并、序列化和安全提交；空闲不占专用线程，不能每功能、每 pair 开线程。普通配置继续沿现有 PreferenceDocument 的机械语义。
- 一份 `world-object-text.json` 保存三个功能的配置，复用既有 `PreferenceDocument` 机械语义。F5 与快捷键都调用同一模式命令；StyleTarget 复用既有颜色/字号弹窗。

## 对象、发现与选择

只读 resolver 从任一组成格的 frame 推回左上角，验证完整 4/6 格，且同类型、合法样式和精确局部 frame。容器 21/441 为 2×2、52 样式，467/468 为 2×2、38 样式，88 为 3×2、65 样式。21/441 使用 `chestType`、467/468 使用 `chestType2`，467 的 style 4 按原版 ChestUI 使用 Item 3988（死人宝箱）；88 使用 `dresserType`。常春藤是 style 10。牌子使用当前 tileSign 且排除 85，当前固定版本覆盖 55/425/573；85 为墓碑。名字优先当前已知非空自定义名，缺信息才用明确通用后备，不把坐标记录当作名称缓存。

发现按当前视口及文字边距渐进扫描，保留可继续的行游标；小幅连续移动不中断进度，大幅跳转撤销旧视口。候选在完整验证后按“当前可见优先、到玩家中心的距离、稳定坐标”比较，最大输出 240/40/40。K 只限制最终输出；更晚发现的更近物体必须替换较远物体，不能扫描先到先得。保留额外候选用于空文案、布局失败或裁剪后的有界补位。尺寸、颜色和移动只使对应投影失效。

当前内部工作上限：每 update 扫描最多 4,096 个视口 tile，同时至多尝试 256 次新对象完整解析；复核已就绪 N≤384（272/56/56）和至多 8 个待准备对象；Opened 查询至多 256 个原始桶/坐标访问，重复或越界也消耗额度；每对象至多 6 格读。相同 tick 的原始格事实共享容量 65,536 的读穿缓存。至多 8 个冷/脏排版工作各消费 512 源 UTF-16 单位，合计 4,096；单标签源消费上限 65,536，单可见单元上限 1,024。达到文字护栏不继续全文扫描。原版 FindChest 已有坐标字典，直接复用，不建立 C 名称索引；长前导空白自定义名共享每 update 512 字符的检查额度，暖命中只比较引用/状态。牌墓共享 S 表游标，每 update 512 槽；32,000 槽完整周期至多 63 次有效更新（已索引的当前候选每次直接复核文本）。不得以统一 elapsed-time 计时器取代可解释工作上限。

只有排版完成且有当前可见墨迹的对象进入上述已就绪名单；8 个待准备槽与 Top-K 分开。槽满时暂停原扫描/历史游标，完成或拒绝后继续原进度，两种发现有工作时轮换优先次序。不能将未知正文先当合格近物塞进 Top-K，再用有限拒绝缓存补救；600 个近处长空白彩色标签超过拒绝缓存后，旧方案会循环重试而饿死远处有效牌子。当前真实原版夹具在 3,200 次更新内完成该反例；它也表明极端连续长空白会推迟后方首次显示，不能承诺所有对象立即出现。普通短名字最多每更新准备 8 件，240 件约需 30 次准备更新，另加附近发现/首次牌表索引的推进时间；移动不无故清空这些进度。

选择用固定最大堆及坐标索引，玩家移动原地重排 N，不为每个被拒候选创建对象。空正文拒绝缓存按坐标/字符串引用最多 512 条，样式/字体/语言/视口排版语义变化后失效；裁剪资格按当前投影单独检查，不记成永久正文拒绝。准备缓存最多 512，逐条淘汰，未再访问的条目最多保留 120 次有效准备更新后每次退役 8 条；取消未完成任务同时撤下缓存，重新出现可重新排队。颜色只更新继承颜色片段；窗口 tile 舍入不作为排版宽度，因此小幅移动不重解析。

三项关闭时不做标签发现、文字、绘制；Always 未取得原版 `accOreFinder` 时先撤销需求再退出；Opened 且已知 H=0 时不做名称表和区域扫描。Opened 使用 64×64 tile 桶查询邻近已打开坐标，桶只因新记录/加载发生变化；不因相机每帧移动重建 H，也不因颜色/字号重新索引。

## 文字与绘制

默认宝箱橙色 FFA500、牌子 E6C16A、墓碑 FF5555，独立字号 70%，70 的默认不改变既有实体 90%。字号范围 50–180、步长 10。牌子/墓碑 Lines 默认 3、范围 1–10；Characters 默认 80、范围 1–1200。All 有最多 10 个实际排版行的护栏，省略号占护栏空间；Characters 按可见完整字符单位计数，颜色标记不计、图标计 1、换行不计，内部空行保留。

有界 lexer 只接受经过验证的原版常见颜色、物品标签，其余按字面显示。一个解析结果供截断、测量和绘制复用；颜色片段之间的组合字符不能被拆开，图标不可分。CRLF 规范为一个换行。基础换行宽度约 460×字号并受视口约束。逐完整单元测量/累积，禁止递增前缀的二次复杂度；长输入未消费部分不用扫描才能判定已经到达显示护栏。

原版 `ChatManager.ParseMessage` 用于已验证标签；缓存 `PositionedSnippet`，Draw 只调用接受该缓存的重载。禁止 Draw 调用 string/未布局 snippet 重载。图标资源取得与布局在游戏线程限额准备；原版 `UniqueDraw` 涉及 `inventoryScale`，外侧 `finally` 恢复。关闭 hover/click 行为。继续借用既有世界 SpriteBatch，禁止每标签 Begin/End 或创建纹理。

颜色仅以安全常量标签建立原版片段，再用 `CopyMorph` 放入已校验字面内容，避免原版正则把彩色正文内反斜杠当转义破坏。每行独立保留实际墨迹范围，内部空行影响行距但不替完全屏外正文占绘制名额；省略时整单元退回并同步可见字符计数。完整 Unicode 单位不被截断不等于原版字体拥有所有字形：原资源预览中部分 emoji 显示为原版后备星号，未新增字体或承诺 emoji 字形覆盖。

## 开过位置与持久化

观察独立于三个显示模式。正常本地玩家、有效会话、背包开启、非负世界 chest 索引、chestX/Y 对应当前 Chest 坐标、完整真实容器才形成事实；拒绝银行、hover、拒绝开箱、其他玩家和 `World.AutomaticOperation`。保持打开同一箱子只比较标量；新坐标才改变 revision，重复坐标不重复写，尚待提交的事实仍归提交 owner 管。

持久 pair 以已载入 `ActivePlayerFileData.Player` 与 `Main.LocalPlayer` 同一对象为前提，角色以 FileData.Path 加 local/cloud 存储域定位；世界以非空 WorldFileData.UniqueId 定位。普通客户端还要求当前连接正常入世，避免握手期旧 GUID。路径/GUID 只在 pair 变化时生成存储键，不逐帧哈希；不读取私人角色文件、显示名、服务器地址、保存时间或完整存档。角色真实文件位置是当前原版可取得的身份，不是角色 UUID：手动把不同角色替换到同一路径无法区分；本地/云移动会形成不同归属，首轮不自动迁移。

身份不足只影响该会话的持久资格，给有限提示，继续记录局部位置，不落到公共 unknown 文件。身份晚到只合并同一 session 的新事实。读取完成后按 pair 与 session/revision 合并，并把加载期间新开的位置保留；旧回调不能写进新世界。离世已接受提交由原 pair 完成，不换绑。

文件为 `JueMingRData/records/opened-containers/<pair-key>.json`，只含 format/version、pair 校验标识及去重左上角位置；不存物品/名称/时间线。单文件有合理容量上限，超限保护原文件并明确降级。后台执行解析、索引构建、合并及序列化；当前查询读已发布索引和本会话增量。Missing、损坏、未来 schema、读取失败、写失败、提交未确认分开，复用原子存储的保护/备份，不因坏文件写空替代。有限去重反馈，无逐开箱成功聊天。

正常写入采用 150 ms 合并窗口，连续新事实最多等待 1,000 ms 后提交；明确可重试失败仅追加 250/500 ms 两次退避，随后等待自然新机会，不永久忙轮询。被冻结并等待合并的会话索引必须随重试批次转交，成功后只退役本次实际覆盖的索引，不能因重试列表已有坐标而丢掉失败状态或永久占满 8 段额度。错误反馈按 pair 的原因转换去重，另一 pair 保存成功不消费旧 pair 的未领失败；同一容量阻塞阶段只发一次，正常重新准入才结束该阶段。原子提交未确认保持保护，不能直接重发或覆盖；退出只有有限等待，不保证强制终止/断电前未提交事实。

## 分步实施与验证

1. 固定需求与 `.8` 原版证据，写精确路径清单，建立 Draft PR。
2. 先建立能证明三条旧缺陷的测试，再实现完整物体/排序选择/设置命令。继续覆盖 off/gate、未知格、边缘组成格、替换与连续移动。
3. 实现有限文字游标、原版标签准备与有界排版，检查 Unicode、未知标记、图标和冷暖路径。接入 F5/统一快捷键及原版世界层。
4. 实现独立观察和 pair 记录，真实隔离文件测试加载期间增量、旧回调、重复坐标、坏文件/未来版本/失败保护与实际消费者。
5. 跑相邻回归和有限性能场景，图形项准确区分设备前不可用与实际绘制失败；测试设施不得混入发布文件。
6. 固定干净提交，独立只读审查完整 diff；按现有完整 Profile 同工作区顺序重复构建/ZIP 比对，安全预检和安装。保留实现 Draft/Issue OPEN，提供少量中文体验卡，等待所有者亲自体验。

测试报告分别列 T（视口 tile）、C（世界 chest 槽位）、H（历史坐标）、N（附近候选）、K（输出）、S（sign 槽位，当前 32,000）与 L（源文本单位），报告典型/尾部工作和可测分配，不推导 FPS 根因或收益。

## 有限成本与验证边界

2026-09-13 的一次性中立原版夹具使用下述固定 `.8` EXE、原 XNB 字体/物品/物体贴图与 x86 XNA 设备。没有创建、初始化或运行 `Main`，没有启动游戏/服务器、读取角色存档或改真实用户数据。独立编译的实际 Host 另经真实类型组合测试，验证唯一 Game 层、F5 控件、共享样式窗、15 个真实动作注册和原 12 项/未知动作保留；源链接适配器测试不冒充这条独立程序集接线。有限计时只在测试项目，Release 不带逐帧探针。可复现入口为 `scripts/test-world-object-text.ps1 -ContentDirectory <匹配原版Content> -OutputDirectory <新证据目录>`；完整开发包使用既有打包器的 `-Profile WorldObjectText`。

下表是一组保留样本 `native-preview-15/finite-costs.tsv` 的结果；源/资源身份和最终候选复跑由 #63 绑定，时间随机器和调度波动。单位为毫秒，分配为当前测试线程每样本平均字节，冷文件单次延迟单独列出。

| 路径与规模 | 做什么／跳过什么 | p50 / p99 / 最大 ms | 分配 bytes |
| --- | --- | --- | --- |
| 三项 Off，C=8,000、S=32,000 | T=0，名称/正文/准备=0 | 0.0012 / 0.0017 / 0.1715 | 0 |
| Always 无能力；Opened H=0 | 各自 T/名称/正文=0，不建全表 | 0.0010 / 0.0045 / 0.1114；0.0012 / 0.0025 / 0.1750 | 0 |
| Opened H=32，附近 N=32 | 600 轮各 256 次原始格请求、96 次名称读取 | 0.0825 / 0.2781 / 0.4015 | 224 |
| 同区 H=32,800，新增 32,768 远处记录 | 查询工作量与上行相同，不逐帧遍历 H | 0.0987 / 0.1250 / 0.1726 | 224 |
| 密集冷发现＋准备，100 轮、最终 N=384 | 渐进发现、资格准备与名单发布 | 0.9300 / 1.8117 / 2.1351 | 25,449.5 |
| 密集站立发现，600 轮 | N 复核、有限 T 和共享 S 推进 | 0.9906 / 1.7498 / 2.5934 | 0 |
| 密集热准备，600 轮 | 复用布局，最后源消费为 0 | 0.1028 / 0.4626 / 1.1374 | 96 |
| 密集实际 Draw，K=240+40+40，300 轮 | 原版已布局描边＋正文；含测试共用 Begin/End/Clear | 1.1224 / 1.5791 / 2.1496 | 76,800 |
| 密集移动更新，600 轮 | 当前可见性/玩家距离重评 | 1.2432 / 2.4158 / 7.7579 | 288.8 |
| 短文 L=28／未知标签 L=5,010／单行 L=1,000,000，各 30 次冷样本 | 累计源工作 32／5,580／302，分别 1／11／1 个 512 单位步骤；非每个长文都扫完 L | 0.0168 / 0.0230 / 0.1760；0.3499 / 0.5926 / 1.1399；0.1713 / 0.1956 / 0.3992 | 10,944／282,500／146,388 |
| Off 下空闲／持续持有同箱，各 600 轮 | 标量观察；空闲存储工厂 0 次；同箱累计仅首次 1 写 | 0.0001 / 0.0001 / 0.0582；0.0001 / 0.0002 / 0.2000 | 0 |
| 同坐标反复关开，600 轮；64 个新事实 | 前者仍只有首次写；后者观察＋完整性＋入队 | 0.0028 / 0.0109 / 0.0661；0.0048 / 0.0106 / 0.0795 | 0；195.2 |

32/32,800 条文件的后台读取、解析、索引、发布到测试轮询分别为单次 12.10/71.64 ms，输入 640/721,536 bytes；这是必要 O(H) 冷成本，不与暖查询混写。64 个新事实合并为一批，加首次事实总共 2 写、1,464 bytes；该批端到端 182.76 ms（包含合并等待、序列化、核验/提交），整个 AppDomain 观察窗口分配 73,728 bytes，包含工作线程，不是逐事实纯保存分配。全历史序列化仍是每次实际保存的 O(H) 后台成本，容量与合并减少无谓重复，未声称增量文件格式或零复制。

320 块原版绘制的 76,800 bytes 即每块 240：x86 原版对已布局 `List<PositionedSnippet>` 的接口枚举器装箱，每次约 48 bytes，四次描边和一次正文共五次。该重载不重新解析或排版，但不是零分配；未为消除它创建复杂池或修改原版渲染。上述 CPU/分配样本不测 GPU 完成、不代表游戏总帧耗时；移动样本最大 7.76 ms 与冷排版分配仍是已报告的尾部成本，不能用中位数掩盖。

真实原资源预览已人工查看正确箱名、相邻同名梳妆台、密集 K/裁剪补位、多行中文/颜色/图标/省略、F5 当前参数与样式窗、屏边/倒重力/缩放。隔离真实文件覆盖加载期间新增、pair 切换、迟到身份、退出、明确失败重试、未确认/未来格式/损坏原件与备份保护。没有可比 Legacy 运行样本，没有 FPS 改善结论；真实游戏体验、普通客机/Host & Play 数据就绪和身份、全部挂放样式、强退未提交历史仍需各自证据。当前安装与所有者接受状态只查 #63。

## 写入清单

以下是首次实现块的精确路径；后续接入块在写入前由本任务补齐，变更必须仍在所有者已授权的同一结果内。

- `docs/设计/宝箱牌子与墓碑显示实现设计.md`
- `src/JueMingR.Platform/WorldObjectText/WorldObjectObservation.cs`
- `src/JueMingR.Features/WorldObjectText/WorldObjectResolver.cs`
- `src/JueMingR.Features/WorldObjectText/WorldObjectSelection.cs`
- `src/JueMingR.Features/WorldObjectText/WorldObjectSettings.cs`
- `src/JueMingR.Features/WorldObjectText/WorldObjectCodec.cs`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/ObjectRulesChecks.cs`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/ObjectSettingsChecks.cs`
- `tests/JueMingR.ArchitectureTests/Program.cs`
- `tests/JueMingR.ArchitectureTests/Phase0TArchitectureChecks.cs`
- `tests/JueMingR.ArchitectureTests/OperationContractChecks.cs`
- `src/JueMingR.Features/Text/VisibleTextBoundary.cs`
- `src/JueMingR.Features/Notes/TextElements.cs`
- `src/JueMingR.Features/WorldObjectText/WorldTextCursor.cs`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/ObjectTextChecks.cs`
- `src/JueMingR.Features/WorldObjectText/OpenedPositionIndex.cs`
- `src/JueMingR.Features/WorldObjectText/OpenedPositionCodec.cs`
- `src/JueMingR.Features/WorldObjectText/OpenedPositionHistory.cs`
- `src/JueMingR.Features/WorldObjectText/OpenedPositionStore.cs`
- `src/JueMingR.Features/WorldObjectText/OpenedPositionQuery.cs`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/OpenedPositionChecks.cs`
- `src/JueMingR.TerrariaHost/World/WorldTileObservation.cs`
- `src/JueMingR.TerrariaHost/WorldTargets/WorldTargetHostObservation.cs`
- `src/JueMingR.TerrariaHost/WorldTargets/HostWorldTargets.cs`
- `src/JueMingR.TerrariaHost/Phase0SLoadChainHost.cs`
- `tests/WorldTargets/WorldTargetObservationChecks.cs`
- `tests/Phase0SFixtureTerraria/Phase0SFixtureTerraria.csproj`
- `src/JueMingR.Features/WorldObjectText/WorldObjectDiscovery.cs`
- `src/JueMingR.TerrariaHost/WorldObjectText/WorldObjectHostObservation.cs`
- `src/JueMingR.TerrariaHost/WorldObjectText/OpenedContainerObserver.cs`
- `src/JueMingR.TerrariaHost/WorldObjectText/HostWorldObjectText.cs`
- `src/JueMingR.TerrariaHost/WorldObjectText/WorldObjectTextWorldLayer.cs`
- `src/JueMingR.TerrariaHost/WorldObjectText/NativeWorldTextLayout.cs`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/ObjectDiscoveryChecks.cs`
- `tests/NativeWorldTextProbe/NativeWorldTextProbe.csproj`
- `tests/NativeWorldTextProbe/Program.cs`
- `tests/NativeWorldTextProbe/NativeChecks.cs`
- `tests/NativeWorldTextProbe/ProbeGraphics.cs`
- `tests/NativeWorldTextProbe/NativeWorldChecks.cs`
- `src/JueMingR.TerrariaHost/F5/WorldObjectControls.cs`
- `src/JueMingR.TerrariaHost/F5/IWorldObjectControls.cs`
- `src/JueMingR.TerrariaHost/F5/F5Layout.cs`
- `src/JueMingR.TerrariaHost/F5/F5Renderer.cs`
- `src/JueMingR.TerrariaHost/F5/F5Shell.cs`
- `src/JueMingR.TerrariaHost/EntityLabels/StyleTarget.cs`
- `src/JueMingR.TerrariaHost/EntityLabels/StylePopup.cs`
- `src/JueMingR.TerrariaHost/Hotkeys/HostHotkeys.cs`
- `src/JueMingR.TerrariaHost/Hotkeys/HotkeyActionIds.cs`
- `tests/EntityLabels/EntityControlsChecks.cs`
- `tests/WorldObjectText/WorldObjectControlsChecks.cs`
- `tests/NativeWorldTextProbe/NativeCompositionChecks.cs`
- `tests/NativeWorldTextProbe/FiniteCostChecks.cs`
- `scripts/build-phase0s-validation-package.ps1`
- `scripts/phase0s/Phase0S.ScriptSupport.ps1`
- `scripts/phase0s/World-Object-Text-Owner-Test-Card.zh-CN.md`
- `tests/Phase0S/Invoke-InstallRecoveryTests.ps1`
- `scripts/test-world-object-text.ps1`
- `scripts/test-phase0s.ps1`
- `tests/JueMingR.ArchitectureTests/WorldObjectText/OpenedFailureChecks.cs`
- `tests/Phase0U/F5LayoutChecks.cs`
- `docs/功能介绍/宝箱牌子与墓碑显示功能行为合同.md`
- `docs/功能介绍/功能索引.md`
- `docs/设计/配置持久化与用户数据布局.md`
- `docs/AI经验笔记/索引.md`
- `docs/AI经验笔记/信息地图与持久状态.md`
- `docs/AI经验笔记/绘制分层与图形资源.md`
- `docs/AI经验笔记/物品箱子与多人权威.md`

记录内部容量：每 pair 最多 262,144 个坐标，单文件最多 8 MiB；最多 8 个仍在处理的 pair，排队及在途列表合计最多 1,048,576 个单位，身份晚到的冻结空间索引另有同量上限且每 pair 最多 8 段。交接不在游戏线程复制或重建历史；超过明确边界的尾部只在原会话有效，必须消费一次的降级通知不能被较早批次成功清除。已接收批次继续受一次在途写入和有限退出等待约束。

## 原版证据

固定 `external/TerrariaRefs/Terraria.exe` 1.4.5.8，SHA-256 `960A03BFF6050CF7BE16DFC1A7B19E10FC2C4F8F835A6A3B135A50DD9E6BA2F3`，MVID `2c29f6c3-4bd9-4add-9c58-da159804e083`。只读 IL/嵌入资源补证：`ChestUI.DrawName 060019F0`、`WorldGen.PlaceChestDirect 060012E6`、`PlaceDresserDirect 060012E7`、`TileObjectData.Initialize 060014DF`、`PlaceSign 06001249`、`CheckSign 06001248`、`PlayerFileData.SetAsActive 06001571`、`Rename 0600157D`、`MessageBuffer.GetData 0600021D` case 7/33、`ChatManager.ParseMessage 06001B2C` 和已布局绘制 `06001B32/06001B35`。不调用有副作用的 CheckSign/ReadSign/Place/交互方法来发现或校验对象。
