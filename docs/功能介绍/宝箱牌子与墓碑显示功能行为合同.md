# 宝箱、牌子与墓碑显示功能行为合同

**状态：Legacy行为与性能形态调查／新版范围待确认／未实现。** 本文是决定下一批范围的候选，整份不是已接受施工图。任务与授权见 [#61](https://github.com/Kuroneko2020/JueMingR/issues/61)，项目阶段见 [#7](https://github.com/Kuroneko2020/JueMingR/issues/7)。

本轮只读提取 Legacy、核对当前 R 与必要的原版版本证据，验证层级为 **V0**。没有运行构建、行为测试、游戏、基准、录屏或安装/恢复；测试源码、历史执行报告与本轮执行严格分开。没有修改游戏安装、用户配置、存档、历史记录或参考原件。

## 1. 一页用户摘要

三项都是把世界物体的文字直接显示出来：宝箱显示容器名称，牌子显示书写内容，墓碑显示墓碑正文。它们不负责打开、编辑、取物、存物或删除物体。

| 旧功能与模式 | 有有效金属探测能力 | 无能力，或旧代码读不到能力 | 需要本玩家开过记录吗 |
| --- | --- | --- | --- |
| 宝箱“始终” `Always` | 扫描屏幕附近容器并显示名称，仍受距离、可读性、原点和绘制限制 | **不取宝箱标签、不扫描该模式目标、不绘制**；即使有记录也不显示 | 不需要；有无记录采用同一查名规则 |
| 宝箱“开过” `Opened` | 按当前角色×世界的记录坐标选候选 | **同样可以显示**，没有金属探测门 | 需要；正在打开的世界箱可能在本次绘制分派前刚登记 |
| 宝箱“关闭” `Off` | 不执行本项标签查询/绘制 | 同左 | 不显示；记录工作是否还跑，取决于共享外层是否被其它世界显示开启 |
| 牌子、墓碑“全部／前几行／前几字” | 读取客户端已有正文 | 同样读取；**两项都没有金属探测门** | 无关，不要求鼠标悬停或打开编辑界面 |

这里的“有效能力”是原版已计算的 `Player.accOreFinder`，不是“是否装备某一个物品”。当前 .8 的携带、合成信息饰品及符合原版条件的队友共享会进入此状态；隐藏原版信息文字不是能力失效。旧帮助方法把 false 和读取失败都返回 false，不能据此解释为“玩家没装备探测器”。见第 3 节及 V8-01。

**显示与记忆是两条不同职责。** 旧记录属于角色×世界，读取本地玩家非负 `chest` 槽及其世界坐标；不是右键意图，也不单独证明用户手动打开或服务端全部数据已就绪。记录入口却挂在共享 Draw：其它显名或五项世界高亮开启，也可能带动箱子导入、记录和保存；所有共享世界显示都关掉时，这个入口不运行。因此旧“关闭仍记”是条件性的，旧记录也不是完整开箱历史。

**已查实的成本包括**：始终模式分步读附近 tile、发布时集中查名；开过模式刷新时整张 `Main.chest` 名称表与当前角色×世界全部记录；逐候选存在性复核、排序、字体度量；记录冷加载、哈希、去重、同步保存及共享 Draw 的附带工作。没有发现本基线为了名称克隆整箱物品；核对的原版 .6/.7/.8 `FindChest` 都是坐标字典查询。所有者“宝箱是性能大户”是明确风险反馈，尚不能归因为某个方法的 FPS 根因。第 5 节逐项列出事实、历史材料和剩余疑点。

**建议下一批保留三项完整用户能力**，复用 R 现有世界层、字体/样式副窗、偏好与公共快捷键。宝箱的记录/身份职责需要独立补齐，牌子与墓碑可先作为一个文字显示子批接通，再完成宝箱子批；这只是实施顺序候选，不删掉宝箱，也不批准实现或旧记录导入。

## 2. 三条真实行为链与操作差异

证据编号在第 8 节固定版本、路径和行段。下表沿真实消费者展开；注册中的 `RequiredActions=None` 不证明整个外围没有文件副作用。

| 功能 | 实际链路 | 独立内容与状态 |
| --- | --- | --- |
| 宝箱显名 | Feature 注册/更多信息页 → 模式 setter 或热键 → `InformationWorldOverlay.DrawInformationInterfaceLayer` → `DrawWorldOverlay` 构造 FullRecord、导入、登记 → `DrawChestLabels` 的 Off/Always 能力门 → `InformationChestLabelService` → tile/名称服务 → 排序、当前坐标过滤 → `InformationWorldLabelRenderer` | 三模式，独立颜色/字号；开过事实由 `PlayerWorldBehaviorStore` 保存。Z01–Z07 |
| 牌子显示 | 注册/更多信息页 → `All/Lines/Characters/Off` 与参数 → 同一外层 → `DrawSignTextLabels` → `GetSignLabels` → 正文布局缓存 → `DrawSignTextBlock`/`UiTextRenderer` | 非墓碑的 `tileSign` 类，独立模式、行数、字数、颜色/字号。Z08–Z10 |
| 墓碑显示 | 注册/更多信息页 → 自己的四模式与参数 → 同一外层 → `DrawTombstoneTextLabels` → `GetTombstoneLabels` → 同一布局服务和最终文本绘制 | `Tombstones=85`，独立于牌子；共享表不代表共享开关或样式。Z08–Z10 |

三项初次默认关闭；旧页面顺序为宝箱、牌子、墓碑。宝箱的 `Known`、`Open` 只是 `Opened` 的旧格式别名，不是额外按钮。旧 enabled 布尔值与模式兼容归一化也会影响异常/旧格式值；正常 UI 关闭同时把 enabled 设 false，不能用人为矛盾字段组合描述普通玩家的第四模式。

牌子与墓碑都实际支持四模式，并非只有墓碑支持按字截取。`Line` 归 `Lines`，`Chars/Character` 归 `Characters`，其它值归 Off。默认前 **3** 行、前 **80** 字；行数 1–10、字数 1–1200，`-/+` 每次调整 1。只有所选模式对应的数量行出现；数量不是源正文编辑限额。

### 2.1 样式窗口与模式恢复

旧三项各自有颜色和字号，不共用值：宝箱 `#FFA500`，牌子 `#E6C16A`，墓碑 `#FF5555`；字号默认 0.70、范围 0.50–1.80、按钮步长 0.10。这些是旧值，不把 R 三实体显名的默认 0.90 或五目标箭头颜色自动套过来。

旧样式窗 HSL 拖动先改局部色块草稿，释放提交；六位 HEX 完整后可自动提交，Enter 也可提交。Esc 可取消当前未完成字段/草稿，却不会撤销先前已提交颜色；不存在统一的“整个窗口确认/取消事务”。恢复默认只针对本项样式，行数/字数是另外的参数。模式/样式写入内存后调用 `ConfigService.SaveAll`，入口忽略保存摘要，不能把成功提示或 `Succeeded` 记录当作磁盘成功。R 已有可靠偏好与失败反馈可以复用，不继承这一缺口。

旧 UI setter 与热键记忆分叉：直接选模式没有同步热键的最后非关闭模式；热键只有在自己从开启转关闭时记住当前模式，关闭状态再按键只恢复其已有记忆，无记忆可能返回 `noLastNonOffMode`。因此“页面选模式→页面关闭→按键开启”不保证恢复刚选模式。新版候选沿 R 的**同一模式命令拥有记忆、按钮与热键共用出口**，保留用户期望的再次开启结果，不复制两份记忆。

### 2.2 外层可达性与退出

F5 是否打开、切到哪一页不是世界标签的运行条件。外层先判断是否存在任一 Information 世界显示需求，再检查 SpriteBatch/原版资源，随后构建上下文；菜单、本地玩家不可取或 inactive 使上下文失败。上下文没有单独的 `dead` 早退，玩家死亡但 active/世界仍可用时可继续经过此链。后续绘制和原版层是否实际执行仍有自己的条件，不能把“死亡”直接推成全部停止。

所有 Information 世界显示都关闭时，最外层 `ShouldDrawWorldOverlay` 在调用服务前返回，连该服务的 Stopwatch 都不创建；只是本批三项关闭、但 NPC 显名或五种高亮之一仍开时，不成立此总早退。只有群系等状态面板开着，也不自动进入世界显示的箱子记录链。世界显示没有因为 F5 关窗而停止；三功能各自 Off 只挡自己的消费者。异常外层 catch 会使后续分派中断，不能声称旧三项已有完整局部失败隔离。

## 3. 宝箱：模式资格、存在、名称与开过记录

### 3.1 模式 × 能力 × 记录表

下面均假定已经通过共享外层资源/上下文门。R 推荐结果另列于第 7 节；这里先记录 Legacy 真实分支。“有效对象”仅指旧 scanner 能确认的 active/type/origin 条件，不等于已核验整件多格物体。“当前打开”是本地非负世界箱槽可读，不是按键动作。Z01–Z06。

| 模式原名/值 | 本机有效探测能力 | 本角色×世界适用记录 | 当前正在打开 | 对象当前可读/有效 | 显示范围与名称来源 | 实际查询/记录工作 | 证据位置 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 始终/Always | 有 | 无 | 否 | 是 | 附近容器候选；客户端已有非空自定义名优先，否则当前类型/样式默认名 | 分步 tile 发现、发布查名、候选复核/排序/绘制；外层先尝试导入与开箱登记 | Z01/Z03；记录 Z05，名字 Z06 |
| 始终/Always | 有 | 有 | 否或是 | 是 | 与无记录同一规则；没有“开过用真名、没开用默认名”的记录分支 | 已有记录不是资格门；正在打开仍每次进入记录去重 | Z01/Z03；记录 Z05，名字 Z06 |
| 始终/Always | 无 | 有或无 | 否或是 | 任意 | 不显示，记录也不能绕过能力门 | 门位于 `GetLabelsForDrawing` 之前；本项扫描/名字/排序不运行；此前 FullRecord/导入/登记可能已运行 | Z01/Z03；记录 Z05，名字 Z06 |
| 始终/Always | 未知/读取失败 | 有或无 | 任意 | 任意 | 旧 bool 帮助方法最终按 false 处理，同上 | 不等于已证明玩家没有能力；typed false 后还会尝试反射字段 | Z01/Z03；记录 Z05，名字 Z06 |
| 开过/Opened | 有、无或未知 | 有 | 否 | 是 | 直接用记录坐标作候选，再取**当前位置当前数据**的名字；1600 世界像素距离及视口相关限制 | 刷新时构造全 `Main.chest` 名字典、遍历本 pair 全部记录；每次输出当前坐标复核；不做 Always 的附近 tile 区域扫描 | Z01/Z03；记录 Z05，名字 Z06 |
| 开过/Opened | 有、无或未知 | 无 | 否 | 是 | 不显示，即使有探测能力 | 刷新仍可能先构造全箱子名表，再发现记录为空；外层导入/登记检查仍存在 | Z01/Z03；记录 Z05，名字 Z06 |
| 开过/Opened | 有、无或未知 | 原无，本次可新增 | 是，且槽/坐标可读 | 是 | 记录若成功进入内存，本次失效重建后可进入候选 | 外层先读 local `chest` 和坐标并去重/保存，再进入标签服务；不是等待新一次用户点击 | Z01/Z03；记录 Z05，名字 Z06 |
| 开过/Opened | 任意 | 有 | 任意 | 不可读、无 active 容器或非原点 | 不显示；不能把旧名字或默认“宝箱”画在空地 | 仍会遍历记录、尝试读坐标；历史记录本身不因此删除 | Z01/Z03；记录 Z05，名字 Z06 |
| 任一开启模式 | 满足本模式条件 | 按本模式 | 任意 | 原点类型/样式变了但仍被认为有效 | 重新计算本次标签位置/名称；**坐标历史可能沿用到替换物** | 过滤阶段可懒建一次全箱名表；底层旧候选未必被替换，后续帧可重复复核/重建 | Z01/Z03；记录 Z05，名字 Z06 |
| 关闭/Off | 任意 | 任意 | 是或否 | 任意 | 本项不显示 | 若另一个世界显示仍开，仍可导入/登记；全部世界显示关时该链不运行 | Z01/Z03；记录 Z05，名字 Z06 |
| 兼容 Known/Open | 任意 | 同 Opened | 同 Opened | 同 Opened | 归一为开过，无额外 UI 模式 | 同 Opened | Z01/Z03；记录 Z05，名字 Z06 |

**失去/恢复能力**：Always 失去能力立即不调用标签服务，保留开启意图与旧内部缓存；恢复后再次按来源签名/安全刷新决定重扫或复用，不是无条件立刻重读全部名字。Opened 不受能力变化控制。能力判断不含锁箱、空箱、`hideInfo`、打开状态或 `Main.chest` 是否存在；这些属于不同事实。

### 3.2 容器类别、位置与名字

| 世界对象 | 旧实际覆盖 | 原点/默认名语义 | 不能推导的结论 |
| --- | --- | --- | --- |
| 普通箱 `Containers=21`、第二套 `Containers2=467` | Always 的 tile 候选；Opened 要适用记录 | 2×2，frame 步长 18，横向样式步长 36；`Lang.chestType/chestType2` | 锁箱仍可默认显名，不表示能够打开 |
| 梳妆台 `Dressers=88` | 覆盖 | 3×2，横向步长 54，独立中心点与 `Lang.dresserType` | 不能当 2×2 箱；梳妆/服装交互不等于打开其储物槽 |
| 假箱 `FakeContainers=441/468` | Always 覆盖，默认名分别同对应箱表 | 2×2；tile 存在不要求有世界 Chest 元数据 | 原版 fake 交互是动画/机关；不能据默认名宣布它有箱内物品或生成正常开过事实 |
| 个人银行/保险箱/护卫熔炉/虚空；钱币槽等便携入口 | 不属于此世界箱标签候选 | 原版当前容器槽 -2…-5 对应 bank…bank4，-1 表示未开；记录入口只接非负槽 | 不能按外形、当前容器对象或“储物空间”一概纳入 |
| 展示人偶、帽架、旧假人、武器架及其它 tile entity | 不属于此功能 | `tileContainer` 同时含展示类，不能拿它当候选集合 | 不读它们的装备/展示内容，不新增标签能力 |

旧 scanner 对单个原点/帧取模与类别作检查，没有核验整件所有组成格的同类型、同样式与正确偏移。相邻合法原点可分别发现，不用连通合组，但残片、异常帧和同址替换不能据此保证完整性或物体连续身份。R 的已有四格目标复核可提供组织方式，不能直接把所有容器硬塞入五目标枚举；梳妆台和牌子挂放变体须按自己的尺寸/帧语义实现。

名字优先级为：**当前可读 Chest 的非空自定义名 → 当前 tile 对应 Lang 类型/样式名 → 宝箱/梳妆台通用名**。开过记录不存名字，不拥有名称真相。Always 与 Opened 取名实现不同，但没有按“是否开过”分配不同名称权限。没同步自定义名、真实未命名、读取失败都可能落到默认名；旧输出没有可靠状态标识，显示默认名不能证明远端确实未命名。

名字缓存使用世界键、坐标、类型/样式、语言及当前 Chest 对象/索引/name 签名；**命中前先查 Chest 并拼签名**。类型默认名先走强类型 Lang 表读取，失败可走反射后备；`Containers2=467` 样式 4 先取物品 3988 的名称，fake 468 没有同一特例。不再使用 `MapHelper.TileToLookup/Lang.GetMapObjectName`，没有循环构造 Item 或为名字读取/克隆物品数组。Opened 刷新另建全表坐标→name 字典。可达链未发现箱内物品快照、容量或交易查询；旧报告若讨论另一条箱内搜索/自动存放路径，不能把成本移植到这里。

外层标签缓存同类型/样式且非空旧名称时直接复用，不主动复核实时改名。Always 来源签名不含语言/name，通常等重扫条件或 300 tick 安全刷新再取新名；Opened 一般等 60 tick/签名失效刷新。名字缓存有语言键，不等于外层标签会立即要求重算。字体变化可刷新度量，却不能自动修正旧语言字符串。

### 3.3 开过事实与保存

记录入口是 `RecordOpenChest` 每次共享世界 Draw 读取本机玩家的 `chest`；非负、对应对象/坐标可读且 X/Y>0，才调用 `TryRecordOpenedChest`。它不读右键、不观察原版请求发出、不看箱内物品、不判断手动/自动来源，也不看其它玩家的槽。正常拒绝/上锁而 local 槽未成功改变不会登记；仅鼠标悬停、自动堆叠接触箱子、fake 触发机关都不由这段代码产生记录。若其它路径确实改变了本机有效打开槽，记录器不能区分其来源。

当前 .8 正常多人开箱是 31 请求，服务端发内容与 33，再由本机 33 消费更新 `player.chest/chestX/chestY`；因此正常链的记录通常在本机收到开箱状态之后，但 Legacy 没有验证整批物品同步完成或请求关联。Host & Play 的本机观察与主机权威必须分开；本轮没有运行三拓扑验证。历史旧键导入本身也可以建立记录，并非所有记录都来自本角色的正常打开事件。

**实际归属不是全局或世界共享。** `InformationWorldContextProvider` 生成 `worldName#worldID`，FullRecord 再读原版 ActivePlayerFileData/ActiveWorldFileData 的 Path/Name。角色、世界分别优先使用规范化路径（Trim、统一分隔符、小写并加 `path:`）；路径缺失才使用 `value:` 加角色名、世界名#数字 ID。文件名是这两个键组合的 SHA-256。这里是代码里的身份来源，未读取任何用户存档正文；不能用项目另一个未被调用的 identity resolver 替它背书。Z02/Z05/Z07。

| 身份变化 | 旧键可以说明什么 | 仍不能保证什么 |
| --- | --- | --- |
| 同路径改角色/世界显示名 | 路径优先可保持同一 pair | 文件被同址替换后仍可能沿用；没有可靠世界内容 generation |
| 同名、不同路径的角色/世界 | 路径均可读时可分开 | 无路径回退时，同名角色、相同世界名与 ID 可能碰撞 |
| 换角色、重连 | 可读 player path 不同则换 pair；重连可能沿用旧 pair | 可用检查只要求键非空，`unknown#…` 也可能进入；不是完整未知身份拒绝 |
| 普通客机、同地址另一存档、Host & Play | 只使用本机上述 file data/回退键 | 没有服务器地址或世界身份协议；未证所有拓扑下 file data 的实际可用性，不能承诺绝不串记录 |
| 同坐标挖掉/换类型/重新放置 | 记录继续保存坐标；显示时读当前位置 | “曾开过此坐标”不证明仍是原物体；新同址物体可能继承候选资格 |

旧 schema 1 文件位于逻辑用户目录 `My Games/Terraria/JueMing-Z/data/behavior/`，按 pair 一份 JSON，保存身份/显示名、创建更新时间及 `(X,Y,FirstOpenedUtc,Source)` 列表。首次访问 pair 会在锁内查文件并反序列化，之后使用进程级字典；本轮定向反向引用没有发现生产退出清理、退出统一 flush 或失败重试入口，只有测试重置。缓存没有发现按世界数淘汰上限，不能因名称缓存有上限就声称所有缓存都有界。

每次新坐标添加后在同一调用中全量序列化并同步 `WriteThrough/Flush(true)`，再 Replace/Move；重复同坐标仍做键计算/锁/线性去重，但不更新首次时间、不再次写文件。写失败只记警告并吞掉，内存已 added，返回可仍为 true；重复开同一箱因此不会触发重试，后续新坐标才可能把整份列表再次保存。加载失败被缓存为空，未知 schema 没有完整保护，未来写入可能覆盖原件；这不是 R 允许继承的恢复规则。

旧 `InformationKnownChestKeys` 导入也从共享 Draw 进入：列表非空且 pair 可用时逐键拆坐标，按完整旧 WorldKey 或末尾数字 worldID 匹配，归到**当前**角色×世界。导入键没有原角色身份，不能证明这些箱是本角色开过。Store 保存失败或本来已存在后，调用者仍可移除匹配旧键并 `SaveAll`，没有跨文件事务，也没有可靠结果反馈。显示开启可能触发文件冷读、导入和写入，不能把“只读标签”推广成全链只读。R 首批候选明确不导入旧记录；目录预留不构成迁移授权。

## 4. 牌子与墓碑：完整正文、截取与数据就绪

### 4.1 目标和刷新

旧服务枚举 `Main.sign`，读 X/Y/text，再读对应 tile。墓碑是类型 85；牌子是 `Main.tileSign[type]` 为真且不是 85 的对象。当前 .8 初始化集合为 55、85、425、573，所以非墓碑消费者还包括告示牌 `AnnouncementBox=425`、破旧木牌 `TatteredWoodSign=573`，不是所有外形像箭头牌的 tile；510/511 不在该集合。这是版本绑定的类别说明，不把所有未知 sign tile 自动纳入新需求。

它不调用 `Sign.ReadSign`，不自动打开界面或发网络请求；没有装备、距离交互资格或鼠标悬停门。显示另受候选范围、1600 世界像素距离与绘制裁切影响。空/全空白正文不生成标签。牌子与墓碑各自调用扫描/候选缓存；同时开启会分别遍历同一注册表，布局缓存才是共享的。

60 tick 缓存命中仍复核当前坐标 active、tileSign 与墓碑类别；tile 移除/失活/换类可以提前停显。**正文没有在命中时重新读取**：别人改字、清空正文、sign 槽消失或同址同类重放，可能到下一次扫描才更新；过滤不改写原缓存，同址恢复还能短暂重现旧字。扫描缓存只用 tick，不含世界、角色或连接代次；本轮未见生产退出 reset，时间倒退能触发扫描，但不能承诺重连/换世界的旧文本绝不短暂串用。

### 4.2 截取的真实含义

| 情形 | Legacy 实际处理 | 边界/缺口 |
| --- | --- | --- |
| 全部 | 归一换行后折行，仍最多 10 个显示行、约 `460×字号` 宽，超出追加 `...` | “全部”不是无限全文；不修改源 `Sign.text` |
| 前几行 | 先按同一宽度折行，再取前 N 个**显示行** | 长单行也能占多行，不能理解成原文第 N 个换行前的全部文字 |
| 前几字 | 先 `Substring(0,N)`，再折行/限制最多 10 行 | N 按 UTF-16 code unit，换行也计数；不是 Unicode 字素数或最终可见字数 |
| CRLF/CR/首尾空白 | CRLF、CR 归一成 LF，整体 Trim | 首尾空行/空格被去除；内部空行保留并占显示行 |
| 中文普通字符 | 按逐 char 宽度折行 | 不证明所有字体均含相应字形 |
| 组合附加符、代理对 emoji、ZWJ 序列 | 逐 char/Substring 切分，没有整体字素边界 | 可切坏字符，旧测试没有证明支持；不作为必须保留行为 |
| 颜色/物品标签 | 截取与度量先面对原始字符串；最终走 ChatManager 字符串绘制，可能解析标签 | 可截断标签，原始串宽度可能不等于颜色/图标布局；不是完整富文本支持合同 |

`1200` 是显示“前几字”参数上限，本文不据此声称本功能编辑源正文或把原文截短到 1200。布局缓存键包含文本/哈希、模式、数量、字号和字体缓存身份；相同文字可共用布局。位置改变不应要求正文重写，旧此链也未写正文。缓存命中不代表最终 ChatManager 不再工作：当前 .8 对需解析字符串仍会 ParseMessage、LayoutSnippets/ToList，再绘制阴影与内容（V8-05）；没有测量就不报告这部分毫秒或 FPS。

### 4.3 位置、层级与非交互

旧文字块以登记坐标乘 16 为左上角、固定宽 32 世界像素，顶端对齐，每行按自身测量宽度居中；X 可夹到屏幕 4px 安全边距，后续行向下排，不为标签增加编辑、拖动或点击命中。扫描拒绝 X/Y≤0，未核验牌子各挂放变体的完整格与 frame/origin。每项最多 40 个成功绘制的文字块，布局准备发生在最终可绘检查之前；这既不是完整注册表处理上限，也不保证首个 40 候选全部可见。候选裁出与最终每行像素裁切是不同工作；标签块边缘、长行、底部超出不因“有范围判断”就证明全量正确裁切。

正常信息世界层在原版主要 UI 之下；F5 与 Notes 的后续界面优先。旧注入还存在锚点不可用时的后备分派，不能把正常层级保证外推到所有 fallback。标签代码主要使用世界坐标减 screenPosition，牌子块自身没有完整倒重力变换，原版 Game 层矩阵、相机/世界缩放与 UI 缩放不能重复施加。新版复用 R 已接受世界层及其投影门，倒重力、特殊视图、边缘换行仍列有限验收，不能把实体短名测试直接当作长正文验证。

### 4.4 单人、房主和普通客机

| 拓扑 | 物体位置/类型 | 箱名与正文何时可知 | 开过事实 |
| --- | --- | --- | --- |
| 单人 | 读当前本地世界 tile；完整性需另核验 | 本地 `Main.chest/Sign` 数据已载入时可读，自定义名与空白仍须分类 | 本机成功进入非负世界箱槽可作为观察依据；显示旧入口可能漏记 |
| Host & Play 本机 | 仍按本机实际可读状态取值；同进程不取消客户端/主机区分 | 可来自本机数据或原版区段/名称/正文同步，不能自动声称一开始全部可读 | 观察本机槽；不是其它玩家开箱，不冒充服务端回执关联 |
| 普通客机/连接专服 | 未同步区段或未知 tile 不等于服务器没有物体 | 正常区段数据可包含箱名、sign 全文；改名 33→69、正文 47 可更新；只消费实际已收到数据 | 正常 33 更新本机打开状态之后可观察，未同步内容与无资格分别表达 |

当前 .8 `CompressTileBlock_Inner/DecompressTileBlock_Inner` 包含箱子索引/坐标/name、sign 索引/坐标/text，所以**未打开也可能已有自定义名和正文**。但没有每项就绪证据时，不能把表中空项当不存在，也不能把默认名标成真实远端名称。

69 是专门名称请求/响应；31 是真实开箱，且服务端正常链可能触发机关；46 请求的 47 在条件满足时会调用本机 `OpenSign`。`Sign.ReadSign` 默认可创建条目，传 false 也不保证无副作用：失效 tile 分支会 `KillSign`。159/`SendSection` 还会同步 NPC 和箱内内容，不是名字专用的轻量接口。本轮只是提取这些语义，**没有授权新版自动请求、模拟开箱/读牌或建设服务器组件**；旧本批显名/正文链未见这些主动网络请求。

## 5. 按模式拆分的成本与旧证据

下面频率是**该调用点可达时**的代码节奏，不是实测每秒次数；一次 Draw 不等于一次 Update，tick 阈值也不自动等于固定墙钟秒数。记附近扫描格数为 T、世界 Chest 槽数为 C、当前 pair 记录数为 H、候选数为 N、sign 注册表槽数为 S、正文长度为 L。它们不可统称“可见物体数”。本基线静态事实为 S0；未运行测试源码断言为 S1；历史执行报告为 H1，不能转成本轮通过或 FPS 证据。

| 操作/路径 | 触发条件/模式 | 实际频率 | 输入规模 | 单次工作及重复关系 | 缓存/失效 | 关闭/无资格时剩余工作 | 证据等级/位置 | R 应避免的问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FullRecord 上下文 | 任一共享世界显示开 | 每次世界 Draw 尝试；file data 通常 60 tick 或 player/world 变化刷新 | 常数字段、路径/名字长度 | 命中前仍读状态、构造 worldKey/签名、加锁；file data 是原版元数据读取，不是每次读存档正文 | context 与 file data 两层；profile 可向 FullRecord 升级 | 本批全关、其它世界显示开仍做；全部共享世界显示关才外层早退 | S0 Z01/Z02 | 普通文字观察不要强迫构造持久身份 |
| Always 目标发现 | Always 且有效探测能力 | 签名变化或完成发布后 300 tick 安全刷新；每次推进最多 2048 格 | T，视口外扩 96px、距离 1696px、边界额外 tile padding | 分步遍历 tile，不是逐帧扫全世界/全 Chest；完成发布另有集中工作 | player/screen 64px 桶、屏幕尺寸、pair/world、颜色/字号等签名；变化会重开扫描 | Off/无资格不取标签，故不推进；外层记录仍可能跑 | S0 Z03/Z04；S1 Z11 | 不让持续跨桶移动反复重启；不要只给 tile 计预算 |
| 坐标→Chest 与名字签名 | Always 发布/按需重算；Opened 其它名字路径分列 | 每个需查名候选 | N，字符串长度 | typed FindChest 后取对象/索引/name；typed 失败可反射重试；.6/.7/.8 FindChest 均字典查询，**未形成逐候选线性遍历 C** | 名称 cache key 生成发生在命中之前；容量 512 FIFO | 分支不运行时不查；其它消费者不因此需读物品 | S0 Z06/V8-02；旧 .6/.7 摘录 | 不能用“缓存命中”抹去前置工作，也不能虚构 N×C FindChest 根因 |
| 默认名/自定义名解析 | 需要新名称 | 按需 | N 与名称长度 | typed Lang/特例 item 名，必要时反射；不构造 Item、不读 `Chest.item` 内容快照 | 512 名称缓存，含语言及对象/name 签名；外层未必立即重算 | 同上 | S0 Z06；H1 H01 | 不能为一个名字复用重型容量/交易查询；地图 legend option 不是箱样式 |
| Opened 标签刷新 | Opened；pair/签名变化、60 tick 或失效 | 每次刷新 | C+H | 先整张 `Main.chest` 建 xy→name；再遍历全部历史，读 tile 后才判距离；没有再扫 Always 的 T 区域 | 标签快照；即使 H=0 刷新也可能先建名字表 | Off 不刷新；无探测仍正常刷新；无记录不等于零工作 | S0 Z03/Z06/Z07 | 不把空记录当完整早退；让远处历史与近处显示成本可区分 |
| 开过签名 | Opened 获取记录 hash | pair 脏或 60 tick | H | 复制记录、再复制/排序并哈希，O(H log H)；随后真正刷新还另取一次记录副本 | hash 缓存；不是每次命中都全 hash | Opened 不取数时无此标签 hash；外层开箱去重仍可能做 | S0 Z03/Z07 | 变更 revision 与正文存储分责，避免同事实多次复制排序 |
| 当前候选复核/排序 | 两个宝箱开启模式 | 每次返回复核 N；来源引用变化或移动 64px 重新排序 | N；发生类型/样式变化时还可能读 C | 当前 tile 检查；排序复制 O(N log N)，优先视口内/距中心/坐标；旧非法源没替换会重复分配过滤数组，样式变化可重复创建标签/名字表 | 稳定排序缓存不取消逐候选复核 | Off/无资格 Always 不运行；Opened 无能力仍运行 | S0 Z03 | 240 只限成功绘制，不限过滤、排序、查名或所有候选遍历 |
| 牌子/墓碑正文准备 | 各自非 Off | 各自 60 tick 或 tick 回退扫描；两项同开分别扫描 | S、所读正文 L | 每个槽取坐标/text，空白检查后判范围；在已有候选上命中仍复核 tile 类别；非全世界 tile 扫描 | 两份候选数组仅 tick 缓存；正文和世界身份不在命中校验中 | 本项 Off 不扫描；另一项不跟着关闭；无探测不影响 | S0 Z08 | 可共享必要源读取，但各自开关/范围/更新仍独立；不得显示跨会话旧字 |
| 正文布局/度量 | 待画文字块，三种开启模式 | layout miss 时折行；hit 仍建键/比较/锁 | L、显示行数、字体度量键 | 逐增长前缀 Substring+Measure，长单行最坏可累计 O(L²) 字符工作；超宽/省略再测；位置不入正文布局键 | 512 布局容量到达后整表清；含文本、数量、字号、字体代次；度量缓存 2048 到界清表 | Off 不布局；相机改变仍需定位但不应写正文/文件 | S0 Z09/Z10 | 只缓存最终行还不足以证明无分配；Unicode/标签截取须有明确语义 |
| 文字实际绘制 | 候选通过绘制门 | 每帧每行/名称 | 成功宝箱最多 240；牌子/墓碑各最多 40 块、各至多 10 行 | 世界投影、测量/定位、逐行绘制；ChatManager 对需解析字符串仍可解析/布局/ToList+阴影 | 字体约 1 秒检查不等于无绘制成本；最终渲染没有因为 layout hit 消失 | F5 关窗仍画；失去 Always 能力停止本项绘制 | S0 Z01/Z09/Z10/V8-05 | 不把限数、延长 TTL 或减少正文能力本身当优化完成 |
| 开箱登记与记录冷读 | 共享 Draw，local 非负 chest/xy 合法 | 正在开箱时每次 Draw 尝试；pair 首次访问冷读 | H、pair 键字符串、JSON 文件大小 | SHA 文件键、锁、线性 Contains；冷时文件查找/反序列化；已有坐标无同值写 | 进程 pair cache；未见生产容量/退出清理 | 宝箱 Off/Always 无能力但其它世界显示开仍可做；全部世界显示关会漏记 | S0 Z05/Z07；S1 Z11 | 独立开过事实观察，别让牌子显示承担 I/O，也别以关显示为由删历史价值 |
| 新记录保存 | 新坐标、导入新增 | 事件触发，但事件入口在 Draw | H 与 JSON 字节数 | 全文件同步序列化/WriteThrough/Flush/Replace；在锁内；失败无可靠确认/同坐标重试 | 保存成功与内存 added 未严格分开 | 同上；重复同坐标不写 | S0 Z07；S1 Z11 | 文件提交与渲染分离；保护读取失败/未知版本原件，限定重试/退出责任 |
| 旧键导入 | 共享 Draw 且旧列表非空 | 每次合格 Draw 可扫未匹配剩余键 | 旧键数+H | 匹配/解析/去重/可能存行为文件，再移除旧键及 SaveAll；不同文件无事务 | 匹配移除；不匹配键仍留下反复扫描 | 三项全关而其它世界显示开仍可能触发 | S0 Z05/Z07 | 首批不做迁移；后续单独授权和证据，不与显示启用捆绑 |
| 诊断及异常记录 | 到达服务、快照消费者或异常 | 每次世界/状态 Draw 各 Stopwatch；读取快照按消费者频率；异常日志 10 秒节流 | 常数计数+开关摘要字符串、多个缓存 getter | World/Status 最终更新同一个 LastDrawElapsedMs；writer 加锁并构造摘要，snapshot 读取更多带锁计数但不重扫/布局；Stopwatch 停止后 writer 开销不在该包络内 | LastFrame/计数；全部世界关闭且外层早退不进入世界 service | 状态面板单独运行会更新同字段；导出不是绘制本身 | S0 Z01/Z12 | Draw 包络不是宝箱名字净耗时，最新值也不是所有层总和；不恢复旧探针 |

### 5.1 具体旧坑、有效修法与历史限制

- **默认名错位**：H01 的 2026-06-05 记录（runtime `1.7.447-chest-label-default-name-fix`）描述用户截图中常春藤箱被叫成猩红祭坛，原因是把地图 legend option 当容器样式。改用 Lang 容器表及 3988 特例是语义修正；报告写有 build/test/audit 通过，但没有本轮重跑，也不证明 .8 运行或 FPS 收益。
- **挖掉仍显名**：H02 与提交 `4258ef07cc5b7efb13b2608191fbfefafa649940` 加当前 tile/category/origin/type/style 复核，避免纯 TTL 留影；不能据此声称完整多格检查、物体连续身份和改名立即更新都解决。H02 的验证段指向最终回复，本次未取得可绑定的完整运行结果，保留为历史修正说明。
- **移动扫描进度**：2048 只限发现 tile；发布阶段对全部候选取名/建数组/过滤/排序不受它约束。64px 桶跨越可重置 partial；首次/世界/pair 重置等待期间可返回空，其它脏因或安全刷新可暂用旧快照。由源码可提出“持续移动可能反复重启、发布延迟”的条件性风险，**未测出实际饥饿时长或 FPS**。R 已有持续移动推进经验，应复用其职责分解再验证本批。
- **命中统计掩盖前置工作**：名字命中前有坐标查询/签名；记录 hash 有复制排序；候选命中有逐格复核；正文 layout hit 仍有最终绘制。H03 的历史调查 A03/A04/A05/A06/A08 本身分别保留真实调用频度、分布、命中、分配/GC、I/O 量及等待缺口。其源基线 `9923966f…` / `0.1166-performance-fps-trend-diagnostics` 不是本轮 Legacy 或 R 运行样本。
- **保存成功假象**：Z11 文件占用测试的断言是 API 返回成功/added 且旧文件未坏，不是数据已经持久提交；旧导入测试禁用 SaveAll 路径，不能证明跨文件迁移安全。这些源码只用于发现覆盖边界，没有执行。

没有本轮同候选、同环境、明确开关/模式与采样边界的真实测量；未取得足以把本批单一路径绑定为 FPS 根因的历史数据。以上风险不授权删除真实显示能力、降低数量代替修复、永久加探针或无限延长调查。以后如获准测量，只使用第 7.3 节有限场景，先分离显示与记录包络。

## 6. 与 R 实际基础的复用对照

基线为 R `fcfd253ea145407495c87b5f4c5654cb703a504c`；实体显名、五目标的接受范围分别查[实体合同](敌怪动物与NPC显名功能行为合同.md#85-实施与接受的边界)、[附近目标合同](附近目标探测与高亮功能行为合同.md)。下面是实际代码可复用边界，不是“界面上有字就已实现”。

| 职责 | 本批真实消费者 | 已有实际实现/入口 | 可复用部分 | 最小缺口 | 必须独立的业务 |
| --- | --- | --- | --- | --- | --- |
| Runtime/Session 与世界退出 | 三显示，箱子开过事实 | `SingleFeatureRuntime`、Host 组合根、`WorldPresentation`（R01） | 生命周期、局部失败与世界绘制资格；Session 结束清事实出口 | 注册本批功能/观察/清理 | 不能让一个文本错误拖停三项或把业务塞回总 Runtime |
| 附近完整对象、可读区段、移动推进 | Always 容器发现、各对象当前复核 | `WorldTargetHostObservation`、`WorldTargetFeature`/resolver（R02） | 可读/不存在分开，视口反投影，tile/section 代次，正在进行扫描不随小幅移动反复重开，当前对象复核 | 容器自己的 2×2/3×2、frame/style，牌子挂放类型；显示文本边距 | 现有 observation 的 accOreFinder 是五目标门，不能直接变成牌子/Opened 的公共门；不扩成五箭头类型 |
| 世界文字与度量 | 宝箱短名、牌子/墓碑文字块 | `EntityWorldLayer`、`UiTextMetrics`、字体身份、Host 同一世界层回调（R03） | 字体/资源就绪、世界坐标与倒重力、借用 batch、层级规则 | 长正文折行、文本更新身份、字素/标签策略、块裁切 | 不把容器变成 NPC，不把正文缓存绑死于实体名刷新周期 |
| 独立样式副窗 | 三项各自颜色+字号 | `StyleTarget`、`StylePopup`、`StyleEditor`、F5Shell（R04） | 已有三实体/五目标样式布局、预览草稿与提交/退出组织 | 新真实 target 与 getter/command；三项默认值待范围决定 | 只复用控件，不自动继承实体0.90字号或箭头颜色 |
| 可靠普通偏好 | 模式、最后模式、颜色、字号、数量、按键 | `PreferenceDocument`/HostEntityLabels 与共同 settings owner（R05） | 合并保存、未知字段/失败状态、内存意图与磁盘结果分开 | 新字段/规范化/模式命令与已有服务接线 | 开过历史不放普通 UI 开关，也不能因存储目录存在就认为历史已实现 |
| 公共快捷键 | 三项再次开关/恢复模式 | `HostHotkeys`、公共 Binding/录入小窗（R06） | 主键沿、冲突设置提醒、唯一录入、既有保存 | 三项稳定 action ID 和统一模式命令 | 不新做按键监听，不重议原版重合仅设置时提醒 |
| F5 信息页 | 实际三行及对应参数 | `F5Layout`、`F5RowLayout`（R07） | 现有名称与模式样板、控件尺寸/命中/小窗互斥 | **当前无真实本批命令**：宝箱已有 config/模式/key 样板，牌/墓仅四模式样板；还要真实参数与样式/键入口 | 占位 `F5Command.None` 不构成可用功能；不重做整套 F5 |
| 开过记录自然 owner | 宝箱 Opened/未来明确消费者 | 当前只有相关存储布局建议；既有物品会话 token 是短期操作身份 | 文件机械提交/后台保存经验可以按需评估 | 本玩家成功打开观察、可靠 pair 身份、只读历史查询、持久确认/退出 | 独立小型开过记录 owner；不让公共 tile 观察或每个 Draw 承担导入/保存，不新造通用数据库/身份框架 |

建议最小事件入口是运行更新阶段观察**本机成功打开的世界箱事实变化**，由独立 owner 去重并拥有当前 pair/未确认保存；显示只消费适用坐标与当前可读名称。普通持久身份未知时先保留当前会话事实并明确降级，不向猜测的永久 pair 写入；可靠自然身份与退出刷新/失败反馈须另行设计。这个方向不保证旧遗漏历史可恢复，也不自动授权完整服务器身份协议。关闭某个显示可停止它的发现/布局，不能清掉其它消费者仍依赖的事实。

## 7. 新版候选、差异与待确认

### 7.1 建议保留的用户结果与有意差异

建议范围包括三个独立功能：宝箱 Always/Opened/Off，牌子和墓碑各 All/Lines/Characters/Off、数量、颜色/字号及公共快捷键；初次默认关闭/未绑定。不把本轮调查理解成已经授权实现。以下为候选，待第 7.2 节决定后才进入设计。

| 主题 | 建议 | 与旧实现的区别及理由 |
| --- | --- | --- |
| 探测资格 | 保持 Always 需要原版有效能力、Opened 不需要；牌/墓无该门 | 使用原版计算结果，不另建物品白名单/队友扫描；未知能力与未知对象数据分别处理 |
| 名字与目标 | 保留普通/第二套、梳妆台及 fake 默认显名；自定义名优先，数据未知时可显示类型默认名而不冒充已知远端真名 | 按实际完整物体分类，锁定/储物/机关资格分别判断；不为名称获取全部箱内物品 |
| 历史与当前对象 | 保存本玩家适用的“曾成功打开”事实，显示前确认当前位置当前物体 | 记录 owner 与显示分开；不把坐标当持续物体身份；不默默继承旧路径碰撞/导入归属错误 |
| 正文能力 | 保留三种显示方式、独立参数与样式，不修改源正文 | 去掉 UTF-16 截坏字符；“全部”的可见长度与标签处理按 Q4 决定，不能声称完整富文本已支持 |
| 模式/保存 | 页面与按键通过统一命令记住最后非关闭模式，复用 R 已接受偏好与副窗反馈 | 不保留 UI/热键记忆分叉、SaveAll 结果忽略、清原缓存导致错误同址复现 |
| 刷新/缓存 | 保持内容和存在性及时反映；世界/连接结束清会话引用，缓存有界且身份足够 | 不照抄60/300 tick、2048格、240/40名额、512/2048清表等数值；不能只调低显示量掩盖处理浪费 |
| 联机与迁移 | 先消费本机已正常收到数据；首批不导入旧记录 | 不模拟开箱/读牌/全图区段请求；原版允许但尚未同步不伪装成不存在，未来任何请求另行明确授权 |

### 7.2 五项产品待确认

这五项只问资料无法代替所有者决定的使用结果；门、字段和旧实际代码已经在前文查明。

| 问题 | 推荐选择与理由 | 其它可选边界 |
| --- | --- | --- |
| Q1：下一批一次交付三项，还是分两个实施子批？ | **先牌子+墓碑，再宝箱，三项总目标都保留**。前两项先用真实功能接通正文/样式；宝箱另处理历史身份与保存，避免为了成组绑进共享 Draw | 同一批三项一起完成；需共同完成宝箱记录契约后才交付 |
| Q2：显示关闭时，新打开的箱子要不要继续记住？ | **继续记录本机成功打开，独立于显示开关和探测能力**；这样后来选“开过”有完整得多的自然历史。身份未知仅保留当前会话、不猜永久归属 | 仅宝箱显示非关闭时记录；用户需接受关着时开的箱以后不入历史。旧“其它显示恰好开启才记”不建议保留 |
| Q3：挖掉后同坐标重新放箱，开过资格是否沿用？ | **延续“开过坐标”**，但先核验当前完整物体，用当前名字；直观且不假装能证明替换物身份，可能使新同址箱继承资格 | 已观察到原物移除/换类后要求重新开；离线替换仍无法可靠识别，不能承诺永久逐物体身份 |
| Q4：“全部”与“前几字”、标签内容希望如何呈现？ | **全部按不改源文的有界正文块显示，明确旧最多10行会省略；前几字按完整可见字符截取；先可靠显示普通文字，颜色/物品标记的保留范围在设计中明确**。避免截坏字与把原始标签误当普通字计数 | 若必须完整长文或原版颜色/物品标记无损显示，把它列入首批能力，再确定长块遮挡/折行规则；不增加编辑器或 Notes 的撤销系统 |
| Q5：首批是否按旧默认外观，并只建立 R 新记录？ | **三项颜色沿旧值、字号0.70，全部默认关闭/未绑定；不导入 Legacy 历史**。保留原外观起点，避开无法可靠归属与跨文件迁移 | 可指定更大默认字号；若必须继承旧历史，另开有身份/回退保护的迁移任务，不能附带实施 |

### 7.3 后续有限验收与性能场景

本节仅是获准实现后的有限验证候选，本轮未执行、未创建探针。测量须固定候选、平台与模式；功能可用、成本形态、FPS/多人及所有者接受分别记录。

| 场景 | 用户结果与反例 | 若随后授权测量，应分开的工作 |
| --- | --- | --- |
| 资格/关闭矩阵 | Always 有/无/失去/恢复能力；Opened 有/无记录且无能力；三项全关与宝箱关但 NPC/箭头开；F5 关窗/切页、active 死亡 | 无资格后的实际发现次数、记录事件、文件访问；不能把保留低频记录判作隐藏绘制 |
| 密集仓库/持续移动 | 相邻箱分别显名，锁箱、梳妆台、fake；持续跨屏后仍能发布，不长期空白或陈旧；视口边缘/相机/倒重力/缩放 | T/C/N 分开，分步推进与发布峰值、命中前查名、排序/复核、实际绘制 |
| 冷历史与重复打开 | 首次 pair、长 H，重复同箱不重复写，Off 新开按 Q2；保存失败可见且不覆盖原件；换角色/世界/重连不串记录 | 文件冷加载、H 去重/复制、序列化/flush、退出与失败恢复；不放入显示净耗时 |
| 名字/正文变化 | 未同步→正常就绪、改名/改字、语言/字体变化、空白、拆除及同址放回；默认名与未知自定义名不混同 | 内容刷新、存在性复核、候选/布局/最终文本解析分别统计 |
| 正文与样式 | 中英文、空行/CRLF、长单行、组合字符/emoji、颜色/物品标记；每项参数/颜色/字号独立；按键恢复一致，小窗提交/取消/保存失败按 R 契约 | L、显示行与度量/分配、缓存清表峰值；不以删文字或一味延长TTL证明优化 |
| 三拓扑 | 单人、Host & Play 本机、普通客机/专服连接；只消费本机事实，未知区段安全退让，不自动交互 | 原版正常数据到达与本功能新增工作分开，不把静态协议提取当多人已验证 |

## 8. 固定来源、可回查位置与缺口

### 8.1 身份与事实层次

| 来源 | 本轮固定身份/范围 | 能支持的结论 |
| --- | --- | --- |
| 所有者明确输入 | #61 原始授权：“宝箱是性能大户”为风险反馈；只做提取/成本形态/R对照 | 调查优先级与边界，不直接证明 FPS 根因；第7节新建议还未决定 |
| R 仓库 | `Kuroneko2020/JueMingR`，main/起始 origin/main `fcfd253ea145407495c87b5f4c5654cb703a504c`；#59 completed、PR #60 已合并 | 当前源码/规则与接入口；不新增对过去实机的接受声明 |
| Legacy 仓库 | 已验证同级 `../JueMingZ` 与 remote `Kuroneko2020/JueMingZ`，HEAD `6ac6356c0c564f43284590c168855e96c50e13f7`，开始时工作树干净 | 下列 Z 编号源码的静态事实；旧 `文档` 原件不受此提交完整绑定，另列内容哈希 |
| 当前原版 | `external/TerrariaRefs/Terraria.exe`，Assembly `Terraria, Version=1.4.5.8`；26,597,888 bytes；SHA-256 `960A03BFF6050CF7BE16DFC1A7B19E10FC2C4F8F835A6A3B135A50DD9E6BA2F3`；MVID `2c29f6c3-4bd9-4add-9c58-da159804e083` | 必要类型/方法/IL 的 .8 静态核对，不是运行游戏；使用已有 ilspycmd 10.1.0.8386 与其 Mono.Cecil，无安装/升级 |
| 旧 .6/.7 精选原版源码 | `references/terraria/1.4.5.6/Terraria/Chest.cs`、`.7/Terraria/Chest.cs`、`.7/Terraria/Sign.cs`，具体哈希见8.4 | 对照旧参考语义，不能冒充 .8 的全部实现 |
| 测试/历史报告 | Z11 为未执行测试源码；H01–H03 为历史文本 | 断言和报告只支持各自范围；无本轮构建、测试、测量/游戏/安装结果 |

### 8.2 Legacy 源码证据索引

除特别注明，路径相对 `../JueMingZ/src/JueMingZ/`，行段绑定上述 Legacy HEAD。每项为定向链路及反向引用，不声称全仓审计。

| ID | 路径、符号与行段 | 对应事实 |
| --- | --- | --- |
| Z01 | `UI/Information/InformationWorldOverlay.cs:19–34`；`Automation/Information/InformationOverlayService.cs:24–89,171–296,304–338`；`Features/Catalog/InformationFeatureRegistrar.cs:26–39` | 最外层门、FullRecord/导入/记录先行、Always 单独探测门、牌/墓分派、绘制上限与声明 |
| Z02 | `Automation/Information/InformationWorldContextProvider.cs:34–75,99–151,244–335`；`InformationPlayerDetectionService.cs:9–23`；`Compat/TerrariaPlayerReadCompat.cs:123–125` | active/菜单边界、身份/file data 缓存、accOreFinder typed/反射与 false/未知折叠 |
| Z03 | `Automation/Information/InformationChestLabelService.cs:12–22,146–199,374–500,515–680,693–737,954–1084,1225–1378` | 模式、Always300/Opened60、2048推进、命中前/后工作、过滤/排序、持续移动与发布 |
| Z04 | `Automation/Information/InformationChestTileScanner.cs:13–70,224–318` | 五容器类型、范围、原点/样式、中心及不完整格验证 |
| Z05 | `Automation/Information/InformationChestRecordService.cs:14–107,135–223` | 本机非负打开槽、pair上下文、导入匹配/移除旧键/SaveAll |
| Z06 | `Automation/Information/InformationChestNameResolver.cs:13–15,41–233,250–366` | 名称查询/缓存键、全表索引、typed/反射、Lang与467/3988特例 |
| Z07 | `Records/PlayerWorldBehaviorStore.cs:34–124,126–205,277–330,399–491,572–619` | schema/路径pair、哈希文件名、锁/去重/复制/排序、冷读/失败/同步保存 |
| Z08 | `Automation/Information/InformationSignTextLabelService.cs:13–126,137–283`；`Config/InformationSignTextModes.cs:7–20` | 两份候选缓存、Main.sign来源、tileSign/85分类、60tick、正文不命中复核；模式常数 |
| Z09 | `Automation/Information/InformationSignTextLayoutCache.cs:150–215,218–378`；`InformationOverlayModels.cs:177–237`；`Automation/Information/InformationWorldLabelRenderer.cs:9–41` | UTF-16截取、折行、最大10行、布局键/容量、位置/距离/裁出 |
| Z10 | `Config/AppSettings.cs:259–280,337–340,638–641,882–883`；`Config/ConfigService.cs:239–282,1022–1023,1673–1692`；`UI/Legacy/LegacyMainWindow.Information.cs:56–179,182–256,341–414`；`Automation/Information/InformationStyleHelper.cs:9–11,67–68,90–91,104–180`；`Input/LegacyUiActionService.Information.cs:201–220,278–297,423–524`；`Input/FeatureToggleHotkeyService.cs:625–652,764–810` | UI参数、默认样式、模式归一、热键记忆、提交与忽略保存结果 |
| Z10b | `UI/UiTextRenderer.cs:549–613,681–807`；`UI/Legacy/LegacyUiInput.Slider.cs:44–80`；`UI/Legacy/LegacyHexColorInput.cs:69–173`；`UI/Legacy/LegacyMainUiState.Window.cs:16–87`；`UI/Legacy/LegacyUiInput.Window.cs:63–90,173–197`；`Hooks/InterfaceLayerHookCallbacks.cs:183–205,309–320,799–818` | 最终文本/字体度量，HSL/HEX、窗口退出/切页、层级与fallback |
| Z11 | `../JueMingZ/tests/JueMingZ.Tests/Program.InformationChestLabelTests.cs:34–165,220–305,308–509,533–715,910–998,1047–1100,1231–1333`；`Program.InformationOverlayUiTests.cs:707–795`及同文件布局断言 | 名字/模式/候选/合成pair/文件失败/导入测试边界、sign存在性断言；未执行，不证明Unicode/多人/FPS |
| Z12 | `Automation/Information/InformationOverlayDiagnosticsWriter.cs:10–58,62–119`；`Diagnostics/RuntimePerformanceDiagnostics.cs:96–119,213–260` | 快照不扫描、嵌套计数锁、World/Status覆盖同一耗时值、滚动样本不是独立宝箱测量 |

### 8.3 当前 .8 与 R 证据索引

IL 的十六进制偏移绑定8.1的 EXE。只列必要符号和事实，不入库反编译源码。已有原始片段 `.local/研究/item-processing-1.4.5.8/MessageBuffer.GetData.cs` 的 SHA-256 为 `34332FDE66F648E8AC838E43718C5DE4375A60A454F5DD05615945CC5ED82570`；其 EXE/tool 来源已记于[原版资料身份](自动存放功能行为合同.md#84-目标-1458-定向原版证据)，本轮仅复用名字/正文/开箱事实片段，不重新研究 #48。

| ID | 固定符号/位置 | 本轮核实范围 |
| --- | --- | --- |
| V8-01 | `Player.RefreshInfoAccs` token `060008E8` IL0048–0101；`RefreshInfoAccsFromItemType` `060008EA` IL02dd–0324；`RefreshInfoAccsFromTeamPlayers` `060008E9` IL0000–009f、0194–01a4 | 重置并由携带/有效装备/原版允许虚空路径及合成信息物品赋值；本机客机有效同队、非零队伍、活跃未死且距离<800的原版共享；此门无 hideInfo 条件。`accOreFinderGold` 是旧后备，未在当前Player字段中找到 |
| V8-02 | `TileID` 常量；`TileID.Sets..cctor` IL0a9f–0ae1；`Main.Initialize_TileAndNPCData1` `06000CE7` IL4a89–4ae8；`Chest.FindChest` `06000081` IL0000–001d；`Player.GetCurrentContainer` `060009E7` IL0000–004e；`TileInteractionsUse` `060009DC` IL03e7–04b5 | BasicChest与fake集合、tileContainer含展示类、tileSign四类；坐标字典、负槽银行、fake机关交互 |
| V8-03 | `MessageBuffer.GetData` `0600021D`，已有片段1770–1884、2268–2309、2963–3004、4385–4391行；`NetMessage.CompressTileBlock_Inner` `060002B0` IL0127–02af、07ad–084d；`DecompressTileBlock_Inner` `060002B2` IL036d–0405；`SendSection` `060002C4` IL0091–0099 | 31/32/33、46/47、69/159本批必要边界；正常区段含箱名/sign全文，SendSection还同步内容；不据此授权任何请求 |
| V8-04 | `Sign.ReadSign` `06000BD1`；`Sign.TextSign` `06000BD2`；`Sign.KillSign` `06000BD0` | Read默认可创建；无效tile会删除，false不是完整纯读保证；32000槽循环为这些原版方法事实，不等于旧每个标签调用它 |
| V8-05 | `UI.Chat.ChatManager.DrawColorCodedStringWithShadow` string overload `06001B3A` IL0017、0082–00c7 | MayNeedParsing及ParseMessage、LayoutSnippets/ToList、阴影/内容；文字布局缓存命中不取消最终解析可能 |
| R01 | `src/JueMingR.Platform/Runtime/SingleFeatureRuntime.cs:21–89`；`src/JueMingR.TerrariaHost/Phase0SLoadChainHost.cs:598–603,857–868` | Runtime注册/退出、Host组合根，不把名称中Single误解为仅容纳一个功能 |
| R02 | `src/JueMingR.TerrariaHost/WorldTargets/WorldTargetHostObservation.cs:18–60`；`src/JueMingR.Features/WorldTargets/WorldTargetFeature.cs:25–75`及同目录对象resolver | 能力门专属现有五目标、section可读、坐标/代次、持续推进/清理；不是现成本批观察服务 |
| R03 | `src/JueMingR.TerrariaHost/EntityLabels/EntityWorldLayer.cs:39–109`；`Rendering/WorldPresentation.cs:7–15`、`F5/UiSurface.cs:9–48` | 世界短名与资源/字体/度量；后两路径相对TerrariaHost |
| R04 | `src/JueMingR.TerrariaHost/EntityLabels/StyleTarget.cs:9–30`、`StylePopup.cs:35–75`、`StyleEditor.cs:24–69`；`F5/F5Shell.cs:64–67,139–166,355–369` | 真实已有样式目标与副窗生命周期；Style前缀文件均在EntityLabels，F5路径相对TerrariaHost |
| R05 | `src/JueMingR.Platform/Settings/PreferenceDocument.cs:33–49,85–151`；`src/JueMingR.TerrariaHost/EntityLabels/HostEntityLabels.cs:26–89` | 偏好实际接入与保存；不证明开箱历史已有实现 |
| R06 | `src/JueMingR.TerrariaHost/Hotkeys/HostHotkeys.cs:16–43` | 已注册当前12项，再建Bindings；无本批三项action |
| R07 | `src/JueMingR.TerrariaHost/F5/F5Layout.cs:208–239,268–271`；`F5RowLayout.cs:13–69`（同目录） | 本批F5占位及None命令，不是已经接线 |

### 8.4 旧文档、历史及未覆盖证据

以下路径相对 `../JueMingZ/文档/`，都是已存在原件；不入库、不替换。旧功能说明中的“用户口述期望”保留为**旧文档转述**，不能升级为本轮所有者决定。资料冲突显式保留：旧牌子/墓碑说明“只缓存文字、不缓存坐标”，但 Z08 另有坐标候选缓存；较早容器笔记分类较窄，后来的显名正文与实际代码确有梳妆台/fake；历史文档不是当前 R 规则。

| ID/材料 | 原件路径/历史范围 | SHA-256 |
| --- | --- | --- |
| D01 宝箱旧说明 | `功能介绍/更多信息页/宝箱显名.md`，更新26-06-13-2327 | `12241023A6D3D5D369C73DA076AB2C4F7E0C73C8285DFFBBEC41D0D4FD36A7A4` |
| D02 牌子旧说明 | `功能介绍/更多信息页/牌子显示.md`，更新26-06-13-2344 | `11570215C42EBD104E8BA57FF255F33CF19A9636B57B11BE2A17874DC851593F` |
| D03 墓碑旧说明 | `功能介绍/更多信息页/墓碑显示.md`，同上时间 | `04EEFA68A040EE5995C4237B495BEC2655C37459FF91E80EB4BCBB43394E592B` |
| D04 容器机制 | `AI经验笔记/Terraria原版参考笔记/Chest_Container_notes.md`，.6背景 | `1388B93E258C9B3370F38BA3B8A4252CCC2B6AEB6D84AC68CA6AF1512B7C0E71` |
| H01 默认名修正 | `更新记录/0537-宝箱显名默认名修复.md`，2026-06-05/1.7.447 | `CA3D6060C7317F07C0CD24E83AE00651FE0EB90B3AFDC7631682666612288DAA` |
| H02 存在性修正 | `更新记录/1.7.656-宝箱显名存在性校验-2606132327.md`；相关源码提交4258ef07… | `6F9AF25F97AD3646D2AB26B8D00A20724B0744D496294BAFEB19BD8C6442688C` |
| H03 历史静态成本 | `归档历史计划/07-信息显示-地图-足迹-搜索-记录性能调查/02-Information状态行世界Label扫描与数据准备.md:103–121`，只读直接相关A03–A08 | `A702AF8BABDDDC3ACE50621969FC7488B1AAD9DE2BA722C35615A74C614F471E` |
| V6 Chest | R `references/terraria/1.4.5.6/Terraria/Chest.cs:387–390` | `D115E4E0145D4E84B1516C33AFBE7D6A282D3BDB518F469B38E26FC912CA008A` |
| V7 Chest | R `references/terraria/1.4.5.7/Terraria/Chest.cs:507–515` | `AE1EDA808B9147286F2726F3684CA1D80258FF7486E4CD01AD7DC0C12537D4E1` |
| V7 Sign | R `references/terraria/1.4.5.7/Terraria/Sign.cs` | `3534CE802205AD556237540461522DA8C53EA5F721BD79040733E0A32B9CC96E` |

未取得或未执行的关键证据：绑定本轮基线的 FPS/分配/保存延迟对照，三拓扑真实数据就绪/身份值，全部多格挂放样式与倒重力长正文图形验证，Unicode/富文本真实结果，以及 H02 指向的完整历史最终执行回执。它们分别限制性能归因、多人身份保证、图形/正文接受和历史通过声明，不阻止本轮形成候选。没有全量读取旧聊天、整个性能计划或用户存档来填补这些缺口；#48继续延期。
