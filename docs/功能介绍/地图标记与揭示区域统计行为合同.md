# 地图标记与揭示区域统计行为合同

- 状态：#85 实现候选合同，未实机接受；不改变已接受功能。
- 任务：[Issue #85](https://github.com/Kuroneko2020/JueMingR/issues/85)。本批 G01/G02；#48 保持延期。交付后等待所有者实测及外部审查，不自动合并。

## 地图标记

标记创建与显示共用一个默认关闭的开关，快捷键默认未绑定。全屏地图一次新右键在当前实际可见地图上选点，弹出八图标选择器：火把、宝箱、魔镜、床、牌子、罗盘、篝火、生命水晶；只是视觉样式，不消耗物品。预览与成品使用同一中心和尺寸。取消、布局变化与失焦不创建标记。

保存连续 tile 坐标及稳定 ID，绝不吸附整格或改变世界内容。无可靠可见几何、非有限倍率、世界外和身份未知时拒绝新建，不猜坐标。首次地图完成绘制后的正常新点击可用；同一输入中的滚轮改变后续视图，不改变已经冻结的点。

每个可靠本地角色×世界最多新建 120 条，每页 10 条。管理窗展示图标、名称、定位、删除；名称双击编辑，最多 10 个完整文本元素，默认本地时间 `yyMMddHHmm`，空名称使用短默认名。重复名称允许。Enter 优先完成输入法，Esc 取消草稿；保存失败保留草稿。翻页、定位与主动关窗等待必要保存成功；删除须二次确认稳定 ID。定位只改变地图视角，保持有效倍率，受合法视角边界约束。

标记是用户资产，与探索摘要和普通偏好分开。损坏、未知格式/字段、重复 ID、身份不符、非法坐标均保护原件。合法超过 120 条完整保留并只读，不截断。未确认提交停写，旧会话回执不能串入新会话。死亡点及标记只共享几何和最终悬停仲裁，业务互不依赖。

## 揭示区域统计

比例为当前本地角色地图中 `Light > 0` 的格数除以当前世界宽×高，分母使用 64 位乘积；不是地图容量、服务器进度、轨迹或团队合并。统计只读，不揭图、不读取地图存档、不产生网络消息。

每个新世界会话从性能模式开始；快速模式只调整完整扫描预算与节奏，不改变暂停或动态偏好。同会话开关地图/F5和死亡不重置模式。暂停/继续与重新统计是独立操作，最多一份完整扫描任务。

完整扫描由游戏线程分批执行。动态开启时通过实际揭示变化标记有限脏块并分批重数；稳定后没有周期性全图扫描。初次扫描和变化使用同一块的替换计数，避免扫描加增量重复。加载、清空、替换、尺寸或身份变化使旧连续性失效，按新的地图代次重建。

动态开关是安装共享普通偏好，没有快捷键。关闭立即停止新增变化处理和自动重数，保留上次结果；明确进行中的完整扫描可以继续，属于时间分摊统计。重新开启需要重建，重复开启不重启。显式暂停不会被模式切换或开关动作解除。

无完整结果显示“统计中…”；历史结果显示“上次统计”；动态有待处理块显示“更新中”；未知不显示 0%。扫描进度与探索比例分开。显示按有限节奏及实际可见变化发布。历史摘要不证明当前地图连续性；新会话不能从旧游标恢复。

动态首次默认已实现为关闭。当前通过有界脏块维护，普通揭示变更不重启全图；已核对 .8 的实际 store 和批次入口。不可取得可靠身份时仅在内存统计，不创建 unknown 共享资产；观察安装失败时拒绝开启动态并提示，可保留手动统计及标记能力。自动证据、设备图形验证、所有者实机与外部审查分别报告，不宣称 FPS 改善。

## 本地验证范围

实际 .8 的 Main.DrawMap/ping 参数流覆盖 72 组缩放/UI/视口/边缘组合；真实输入 postfix 验证冻结点、保存确认、定位及焦点/原版输入接管。真实 XNB 与图形设备又完成 72 组 codec 重载后像素范围对照原版 MapOverlayDrawContext，差异不超过 1 个渲染像素；管理/名称/短窗口/八图标/占位/选择器夹边及死亡点共存图已检查。合成中文 IME 的 Enter/取消/晚到字符有自动检查，实际 Windows IME 操作仍待所有者。

实际地图更新、Load/Clear/UpdateType 和 LightingEngine 并行写入与独立枚举答案对照；大小地图、三种观察状态、六种变化和详情显隐分别运行 2000 次。大地图与 120 标记/1024 死亡点/动态变化共同运行 2000 次共享回调，并验证离会话释放地图。保护损坏/未来/身份冲突/迟到和未确认保存，保留失败草稿；完整安装恢复套件在隔离目录核对新旧 profile 与用户字节保留。具体输入、预算、成本、测试入口见[实现设计](../设计/地图标记与揭示区域统计实现设计.md)，最终源码/包/安装身份查任务 Issue。

这些是隔离自动检查及设备资源检查，未自动操作真实游戏、探索地图或改用户存档；普通多人客机、实际输入法/焦点体验与 FPS 对照均未作为通过项。独立代码审查与所有者最终实测保持独立。

## 本批退出

完成独立审查、固定候选完整包和安全安装；保留原用户数据及安装供实测。交付可操作清单及未验证边界，Issue 保持开放、PR 保持 Draft。
