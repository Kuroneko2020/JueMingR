# JueMingR 文档入口

本文件是 JueMingR 的正式文档入口和未来路由表。每个分类只承担下列唯一职责：

- `product/`：产品目标、用户能力和兼容契约；
- `architecture/`：系统边界、运行时模型、模块结构和性能模型；
- `governance/`：开发、测试、性能、依赖、审计和发布规则；
- `migration/`：Legacy 功能目录、配置迁移和行为差异；
- `decisions/`：重要架构决策记录；
- `plans/`：已经批准的实施计划。

这些分类当前尚未建立。后续必须经过设计评审后按需逐步创建；现在不得创建空目录或占位文件。
