# 地图标记与揭示区域统计 · 实测卡

这是 #85 的完整开发候选，包含此前已接受功能。默认关闭地图标记与动态统计；地图标记快捷键默认未绑定。安装后保留现有设置、笔记、死亡记录和其它用户资料。完成以下观察后，用普通文字告诉我结果即可，不需要解读 JSON 或打分。

## 标记

1. 正常启动游戏进入常玩的角色和世界，按 F5 到地图页，打开地图标记。打开全屏地图，在容易辨认的位置右键，选择一种图标。对照选点是否准确，试一次地图边缘、非整数缩放和平移后立即点击。
2. 依次看看八种图标；取消一次选择，确认没有新增。试长按右键，确认不会连续新建。检查原版拖图、ping、玩家图标和已开启的死亡点及悬停文字仍可使用。
3. 在 F5 的标记管理中双击名称，输入中文和组合字符。名称最多 10 个汉字或 20 个英文字母，混合输入按同一长度计算；Enter 先确认输入法，再次独立按 Enter 保存，Esc 取消草稿。试翻页、关闭和定位，确认保存完成后才执行。容量在标题右侧，底部显示“第 x / y 页”；窗口较矮时可以滚动列表，底部按钮保持可用。
4. 定位应只移动地图视角，不移动角色；有效缩放应保留。试删除并取消确认，再确认删除；切换世界后不能出现另一世界的标记。
5. 正常退出再进入，核对图标、位置、名称仍在。需要快捷键时，通过现有公共快捷键窗口绑定地图标记；检查全屏地图里按键可以关闭选择器及标记。

## 揭示区域统计

1. 菜单统计状态应在功能名右边。详情应为较窄的窗口，关闭按钮在右上角，三行控件在同一框内等距排列、左右对齐，按钮不贴边框；悬停动态开关可看到说明。揭示比例应比按钮文字更醒目；没有完整结果时，“统计中…”与“已扫描：xx.xx%”同排，扫描进度与揭示比例是两件事。新世界会话使用性能模式，小世界约 1.4 分钟、大世界约 5.6 分钟；快速模式约 3～11 秒（按每秒 60 次更新、无暂停和竞争估计）。
2. 在详情中切换快速模式，暂停、继续，再重新统计。确认暂停不被切换模式或动态开关偷偷解除；同一会话关闭 F5/地图再开不会重置模式。
3. 在“动态更新”行点击“已关闭 · 开启”，然后正常去一片未揭示区域，再停止移动等待追赶。比例旁的“更新中…”应在追赶完成后消失，不会因为只是走动就反复增加。探索比例很小时，两位小数暂时不变是正常的。
4. 有可靠结果后手动重新统计并暂停，在动态仍开启时继续探索，动态结果应继续更新。首次完整扫描尚未结束时不应冒称已有完整比例。
5. 点击“已开启 · 关闭”后继续探索，详情显示保留比例及“上次结果”；重新开启应重建。完成后不再出现“完整扫描已结束”。重进世界重新从性能模式开始，动态更新开关按已保存偏好恢复。
6. 如方便，再观察普通多人客机的本地地图。此候选没有新增网络请求；多人正确性仍需实际观察，当前未宣称实机多人通过。

## 相邻功能与反馈

短查此前常用的 F5、笔记、快捷键、物品、实体/世界标签、方向提示、信息摘要和死亡记录。特别试一条视觉折成两行的笔记标题，其方向键、Home/End 应按视觉行工作。

若出现问题，告诉我“在哪一步、预期怎样、实际怎样”，附可见截图或已有日志即可。不要为测试故意损坏真实文件、清空地图或删除存档。出现保存失败/未确认提示时保留现场，不反复覆盖重试。安装保持不动，等待本次实测和外部审查；本批不自动合并、发布或进入下一批。
